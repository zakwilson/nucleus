; Stage 12 N5: facade namespace that re-exports geom symbols.
; `export` makes gfacade/area and gfacade/perimeter resolve to
; the same Syms/ir-names as geom/area and geom/perimeter.

(ns gfacade)

(import-prefixed nsgeom geom)
(export geom/area geom/perimeter)
