(include stdio)

(defn main:int ()
  (let (i:int 1)
    (while (< i 5)
      (cond (< i 3)
        (printf "small: %d\n" i)
        true
        (printf "big:   %d\n" i))
      (inc! i)))
  (return 0))
