(import-use "stdio.h")

(defn main:int ()
  (dotimes (i:i32 4)
    (let (cur:i32 (+ i 1))
      (cond (< cur 3)
        (printf "small: %d\n" cur)
        true
        (printf "big:   %d\n" cur))))
  (return 0))
