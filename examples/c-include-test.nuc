(import-use "stdio.h")
(import-use "stdlib.h")
(import-use "string.h")

(defn main:i32 ()
  (with (s:ptr (malloc 64))
    (memcpy s "C header import works" 22)
    (printf "%s\n" s)
    (printf "len = %ld\n" (strlen s)))
  (return 0))
