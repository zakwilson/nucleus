(defenum Color RED GREEN BLUE)
(defconst MAX_VAL 255)

(import-use "stdio.h")

(defn main:int ()
  (printf "RED=%d GREEN=%d BLUE=%d MAX=%d\n" RED GREEN BLUE MAX_VAL)
  (cond (= GREEN 1)
    (printf "GREEN is 1\n"))
  (return 0))
