; Stage 14 AVR-6 — `:const` is meaningful ONLY on a defvar global. A let binding
; has no independent global-vs-constant storage class to select, so `:const` here
; is a targeted compile-time error rather than a silently-ignored attribute.
(defn main ():i32
  (let (:const x:i32 5)
    (return x)))
