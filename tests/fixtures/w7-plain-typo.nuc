; Stage 15 W7 boundary: no local named `zz`, so the message must stay the plain
; unadorned one — the hint must not leak onto an ordinary typo.
(import-use "stdio.h")
(defstruct Point x:i32 y:i32)
(defn main ():i32
  (let (p:ptr:Point (alloca Point))
    (printf "%d\n" (p zz))
    (return 0)))
