; Stage 15 W8 G-2: a pointer-to-array. The permission is consumed by the OUTER
; type parse, so the nested array is refused — pointer-to-array has no use in a
; language where every array read already decays to a pointer to element 0.
(defvar g:ptr:(array i32 4))
(defn main ():i32 (return 0))
