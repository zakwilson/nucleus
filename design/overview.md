Nucleus is a replacement for C using Lisp style syntax and macros with LLVM as its target. It is a low level systems programming language meant to manipulate memory directly, and when required, unsafely.

## Goals

### True drop-in substitute for C

- Zero mandatory runtime overhead relative to C
- C interop - include C libraries and call C functions with no overhead
- Use C structs and arrays natively

### Lisp syntax

- The language is written as a direct representation of linked lists of symbols, numbers, and strings
- Reader macros add syntactic sugar for things like dereferencing pointers
- User-defined reader macros will be possible
- A vector type like Clojure could be useful, or at least improve readability
- Member access for arrays and structs by calling them like functions - maybe provide a way to overload this

### Full language at compile time

- Macros run at compile time have full access to the Nucleus language, as in most Lisps
- Language features and libraries required for macro expansion may not be necessary at runtime
- Something like Common Lisp's eval-when or a compile-time-only include form is probably required for some advanced macros

### Optional compiler at runtime

- It must be possible to compile and load code at runtime
- Because it adds overhead, the compiler is available as a library
- This allows eval, a REPL, and the ability to interrogate compile-time metadata during development
- For development, a REPL harness can load code files so the developer can use the REPL without the code including it

### Macros are the killer feature

- Macros allow significant abstraction at compile time without requiring runtime overhead
- Reader macros allow sugar for common syntactic patterns

### TODO/undecided:

- Syntax for storage class specifiers — resolved by the keyword-attribute slot: [stage14/attributes.md](stage14/attributes.md). `:volatile` is fully migrated (AT-1/AT-2/AT-3 all done: the slot, example/doc migration, and retirement of the old postfix spellings as targeted hard errors); `:thread-local` is reserved with a diagnostic (blocked on a threading stage); `:static` locals and `:align`/`:section`/`:weak` remain sketched only.
- Other metadata or compiler hints — same design (the keyword-attribute slot)

## Design principles

- Small core - it's better to have few builtins and more libraries
- Few special forms - it's especially better to have few builtins with special behavior
- Many macros - where ergonomics or expressiveness require special behavior, macros are the preferred implementation
- Macros vs functions - In Lisp, it is conventional to prefer functions over macros wherever possible. In Nucleus, runtime overhead is is equally important.
- LLVM native - Nucleus targets LLVM and relies on its infrastructure to simplify implementation when possible.
- C-compatible - Nucleus is a drop-in replacement for C with a superset of C's functionality. It must be able to use any C library's functions and data structures, but not its macros. Likewise, any functions and data structures from a Nucleus library must be consumable from C.

## Design documents

- [initial.md](initial.md) — the stage-0 target program and expected output
- [stage0-plan.md](stage0-plan.md) — implementation plan for the stage-0 compiler (C host, LLVM IR backend)
- [stage1-self-host.md](stage1-self-host.md) — implementation plan for self-hosting the compiler
- [syntax.md](syntax.md) — notes on syntax
- [stage2-macros-jit.md](stage2-macros-jit.md) - early features following successful self-hosting: macros and JIT
- [stage3b-interop.md](stage3b-interop.md) — C interop: unsigned types, function pointers, C header parsing, `--emit-cheader`
- [stage3c.md](stage3c.md) — deferred C interop issues: unions, bit-fields, struct ABI, platform portability
- [stage6-cleanup.md](stage6-cleanup.md) — REPL, printing, macroexpand, n-ary arithmetic, modularization
- [stage6-plan.md](stage6-plan.md) — implementation plan for the non-blocked items in stage6-cleanup.md
- [stage6-floats.md](stage6-floats.md) — design for `f32` / `f64` types (deferred from stage 6)
- [stage6-redefinition.md](stage6-redefinition.md) — design for REPL function redefinition (deferred from stage 6)
- [stage9/polymorphism.md](stage9/polymorphism.md) — Stage 9 polymorphism: the dispatch engine. §§1–8 survey the options and the chosen hybrid (overloaded `defn` multimethods + protocols as a thin checked layer); §9 is the authoritative build spec with landed implementation status (rungs 1–4); §10 specifies the planned **engine extensions** — blanket protocols + `Any`, inferred structural bounds (`Valid`), and operators-as-ordinary-functions (`Num`/`Eq`/`Ord` + user operator overloading); design decisions resolved at the end
- [stage9/callable-values.md](stage9/callable-values.md) — Stage 9: what happens when a non-function is in the call position. **Landed** (impl-status section at end) — `(callee …)` desugars by argument type to `get` (symbol selector → member access, the struct default via the `Struct` blanket protocol, byte-identical to `.` and overridable) or `invoke` (general call; `Seq`/`Call` library protocols in `lib/seq.nuc`, e.g. integer → element). Field selectors are symbol *values* (literal → zero-overhead static GEP; computed → runtime field dispatch over homogeneous structs). Arbitrary-expression heads work and a fn-pointer head folds to `funcall`. Builds on the engine extensions in polymorphism.md §10
- [stage10/safety.md](stage10/safety.md) — Stage 10 umbrella: safety without breaking C compat or adding mandatory runtime cost. Resolved decisions (scope = lifecycle + nullability; non-null via opt-in `(ref T)` then flip; `unsafe` as a naming convention), shared invariants, and the L1→N1→L2→N2→L3 staging. Links the two workstream specs
- [stage10/lifecycle.md](stage10/lifecycle.md) — Stage 10: the `with` lifecycle. Escape/taint analysis in `node-type` (a `with`-owned pointer escaping via return or store-out is a compile-time error; deref/field copies are fine), a `Drop` protocol generalizing the libc-allocator special-case (+ `defer`), and `move` for ownership transfer (replacing the `(set! p null)` disarm) with a double-free check
- [stage10/nullability.md](stage10/nullability.md) — Stage 10: non-null `(ref T)` and nullable `(Maybe (ref T))` / `?T` (niche-encoded, C-ABI-identical to `T*`). Transition forms (`if-some`/`unwrap`/`unwrap-or`/`some`/`none`/`as-ref`), flow narrowing so existing `(when (= x null) …)` guards *are* the narrowing mechanism, and the opt-in-then-flip migration
- [stage10/flip.md](stage10/flip.md) — Stage 10 **Phase F build plan** (the safety flip, **landed**): the 5 parser edits, the measured ~343-site scope, the enumerate-then-batch strategy, and the byte-identity rationale. Implementation note (nullability.md §9.1): flipping the elem-less `ty-ptr` singleton overshoots to ~733 violations, all into untyped `void*` slots; `pkind-flow-check` exempts elem-less destinations so the flip's non-null teeth land only on typed `(ptr T)`/`(ref T)` pointers — the direct analogue of the CStr-is-ref-compatible refinement
- [stage10/errors.md](stage10/errors.md) — Stage 10: **error-handling options survey** (no decision resolved). Error values / error unions mirroring `?T` (niche-encoded ERR_PTR-style `!T`, `deferror`, `try`/`if-ok`/`errdefer`), generic `(Result T E)` gated on sum types, traditional exceptions (sjlj and zero-cost tables), conditions/restarts (full and resumption-only "C-lite", with a library sketch and a standalone-vs-combined-with-A comparison), status-code baseline; tradeoff matrix over C interop, runtime overhead, implementation complexity, versatility, and `with`/`defer` cleanup integration; proposes the two-tier values-plus-`die` shape and rejects exceptions. **Decisions resolved** at the end (A1 built; `!`/`!?`/`?!` sugar; global error ids; handler-aware `err` with `err!` opt-out; safety flip folded into this pass)
- [stage10/errors-prompt.md](stage10/errors-prompt.md) — condensed, actionable implementation prompt distilled from errors.md's resolved design: the authoritative decisions and invariants, then phase-by-phase build order — **E1** (`Err`/`deferror`/descriptor table, prelude `Result`, `!T`/`!?T`/`?!T` sugar, `try`, `err!`, `unwrap`), **E2** (value-`Maybe` + `?!T`), **E3** (handler chain library + `with-handler` + compiler-emitted handler-aware `err`), **E4** (adopt `!T` at `die-at` sites), and **Phase F** (the safety flip: `(ptr T)`→non-null, raw→`(raw T)`, `?`→uniform `(Maybe T)`, using the N2 friction findings as the map). Source-anchored to the U2/U3/N1 machinery it reuses; lists out-of-scope items (A2, `if-ok`, `errdefer`, standalone `signal`, `dyn`, exceptions)
- [stage10/cleanup-prompt.md](stage10/cleanup-prompt.md) — condensed, actionable implementation prompt for the **deferred tail of Stage 10**: **C1** (N2 cold-site cleanup — the ~25 nullable-launder `cast ptr:Sym` waivers), **C2** (standalone `signal` over the existing handler chain — call a handler outside return position), **C3** (E4 coercion-path adoption to `!T` for closed recoverable sub-graphs + a panic-tier `signal 'unhandled-error` hook), and **C4** the capstone (the niche layout engine: U4 `&repr` + rules 1–3 and A2's ERR_PTR encoding, sharing one `union-layout-classify`, with a typed-`(ref T)`-payload guard that keeps the reader's elem-less `!ptr` byte-identical). Lists authoritative decisions, the zero-cost / C-ABI / byte-identity invariants, and out-of-scope items (cleverer niches, whole-emitter `!T`, `dyn`)
- [stage10/unions.md](stage10/unions.md) — Stage 10: untagged unions + tagged sums, the type errors.md's A1 needs. Layer 1: raw `(union …)` (C parity — unblocks the stage-3c header skip, SysV class merging in `abi-classify`); layer 2: `defunion` tagged sums (`{tag, union}` struct, generated constructors, C-legible export) + `match` with exhaustiveness checking lowering to `case`/`switch`; explicit-instance templates for `(Result T E)` (memoized stamping, the constructor target-typing question); niche layout rules under which `?T`/`!T` become layout instances; U1–U4 staging
- [progress.md](progress.md) — overview/index of done / pending / deferred work; per-stage detail links below
- [stage8/progress.md](stage8/progress.md) — Stage 8 detailed progress tables (Phases A–F: target descriptor, multi-target backends, SysV ABI, long data model, struct layout, Windows build)
- [stage9/progress.md](stage9/progress.md) — Stage 9 detailed progress tables (polymorphism rungs 1–4, blanket protocols, callable values, Stage 9 cleanup)
- [stage10/progress.md](stage10/progress.md) — Stage 10 detailed progress tables (unions U1–U4, error handling E1–E4, safety flip Phase F, cleanup C1–C3)
- [stage6-rest-optional.md](stage6-rest-optional.md) — design for `&rest` / `&optional` parameters in `defn`
- [stage6-expressions.md](stage6-expressions.md) — design for expressions-as-values and implicit return
- [stage6-pointer-syntax.md](stage6-pointer-syntax.md) — replace `*T` pointer syntax with `(ptr T)` constructor
- [long-term-issues.md](long-term-issues.md) - potential problems for a mature implementation, deferred during early phases of development
- [stage7.md](stage7.md) — Stage 7 ergonomics overview; sub-plans: [implicit-cast](stage7/implicit-cast.md), [optional](stage7/optional.md) (`&optional` args), [macro-casts](stage7/macro-casts.md), [repl-features](stage7/repl-features.md), [interaction-mode](stage7/interaction-mode.md) (Emacs)
- [stage8.md](stage8.md) — Stage 8 C-parity overview, with [stage8-list.md](stage8-list.md) (candidate C features); sub-plans: [types](stage8/types.md), [globals](stage8/globals.md), [expressions](stage8/expressions.md), [platform](stage8/platform.md) (target descriptor + cross-compilation + struct ABI — Phases A–F), [volatile-plan](stage8/volatile-plan.md), [anon-struct-plan](stage8/anon-struct-plan.md), [cheader-struct-plan](stage8/cheader-struct-plan.md), [optimization](stage8/optimization.md)
- [stage888-deferred.md](stage888-deferred.md) — items deferred (C interop boundaries, stage-10 error-handling tails, strings/Unicode; pruned 2026-07-02 of done/designed/obsolete items)
- [stage9/cleanup.md](stage9/cleanup.md) — Stage 9 cleanup (`case` macro, error attribution, one-symbol-one-kind, target descriptor) and [stage9/implementation.md](stage9/implementation.md) (compiler implementation notes)
- [stage999-future.md](stage999-future.md) — far-future / wishlist work (declarable blanket protocols, gensym reader macro, slices/bounds-checking/`const` pointers, other editors, misc base features; pruned 2026-07-02 — lambdas/closures/polymorphism/collections/strings/redefinition/import-only all landed, several errata absorbed into stage-14 designs)
- [stage11/collections.md](stage11/collections.md) — Stage 11: mutable/low-overhead (STL-spirit) collection types as libraries (`Vector`/`HashSet`/`HashMap`/`String`) + core protocols (`Coll`/`Seq`/`Assoc`/`Set`/`Hash`/`Str`). Lazy iterator transforms (`map`/`filter`/`reduce` over an `Iterator` whose `next` returns `(Maybe E)`; `into`/`doseq`), an explicit-choice-with-default `Allocator` protocol (Zig-shaped) + `Drop`/`with` ownership, `usize`/`ssize` indices, reader-macro literals `[…]`/`{…}`/`#{…}`. Prereqs and end-of-stage compiler adoption called out
- [stage11/parametric-structs.md](stage11/parametric-structs.md) — Stage 11 prerequisite: generic structs `(defstruct (Vector T) …)`, the deferred-since-Stage-9 parametric-generics rung. A struct analogue of the existing `defunion` templates (`register-union-template`/`union-template-stamp-types`/`subst-tyvars-node`) reusing the rung-4 monomorphizer for methods; mangled-name stamping + memoization, deferred-IR emission, the type-application-vs-compound-literal construction crux, parametric-protocol conformance (`(extend (Vector T) (Seq T))`), SysV ABI + `.nuch` template export, and the `usize`/`ssize` scalar prerequisite
- [stage11/parametric-structs-prompt.md](stage11/parametric-structs-prompt.md) — condensed, actionable implementation prompt for parametric-structs.md: authoritative decisions + invariants (zero-overhead, byte-identical bootstrap since the registry is inert until a stamp fires), then tasks **T0** (`usize`/`ssize` scalars) → **T1** (template registry + stamping, the core) → **T2** (methods, tyvar-from-receiver + monomorphizer) → **T3** (construction + compound-literal ambiguity) → **T4** (parametric-protocol conformance) → **T5** (C ABI + `.nuch` export) → **T6** (examples/docs/progress), each scoped to specific functions and assigned to a local subagent (systems-impl-engineer / focused-task-implementer / build-test-runner / api-docs-writer) with a build-test-runner gate after each
- [stage11/progress.md](stage11/progress.md) — Stage 11 detailed progress tables: parametric structs (T0–T6 — `StructTemplate`, `register-struct-template`, `struct-template-stamp-types`, `sanitize-for-c`, etc.) and collections (M1–M5 done — `Allocator`/`AllocHandle` + libc/arena backends, `Iterator` + lazy combinators, `Vector T` (`Coll`/`Seq`/`Drop`), the `Hash` lib + `HashMap K V`/`HashSet T` open-addressing tables (`Assoc`/`Set`/`Coll`/`Drop`), and the reader-macro literals `[…]`/`{…}`/`#{…}` (kind-based element inference, `let`+init+conj expansion); the `Seq`→`IntIndexable` rename, the aggregate-`defvar` `zeroinitializer` fix, the deferred static-conformance/import-ordering limitation, and the `COMPILER_DEPS` Makefile fix; M6 `String` + literal switch remaining)
- [stage11/string.md](stage11/string.md) — Stage 11 M6 `String` design (evaluated + decided): UTF-8, memory-safe, "thin wrapper over `(Vector ui8)`". Three read layers — **`ByteStr` ⊂ `Str` ⊂ `String`** — over the borrowed `StrView` substrate: `Str` is **read-only, extends `Eq` only** (not `Coll`/`Seq`/`Drop` — mutation/ownership live on `String`); byte index O(1), codepoint index O(n); `chars`/`bytes` are associated-iterator protocol methods (spike-validated, `examples/assoc-iter-return.nuc`). `Char` = a **new built-in distinct scalar** over `ui32` with `\a`/`\newline`/`\u{…}` literals + UTF-8 encode/decode (UTF-16 → stage888). Bounds/validation via **error-handling** (`!T`: `str-index-out-of-bounds`/`invalid-char-boundary`/`invalid-utf8`/`invalid-codepoint`); `parse` = `FromStr` with explicit target type; `String`/`StrView` are `Ord`. String literals migrate to a **borrowed static `StrView`** (not owned `String`) at end-of-stage. Decisions table + resolved Qs at the foot of the doc
- [stage11/string-prompt.md](stage11/string-prompt.md) — condensed, actionable implementation prompt for string.md: required reading (incl. `collections.md` memory model, `assoc-types.md` + the value-return spike) + delegation workflow + ground-verify (assoc-type methods, `StrView`/`Vector ui8`, and that no `Char` scalar exists yet), then tasks in dependency order — **S0** (the `Char` built-in scalar + char-literal reader, the critical path, modelled on `CStr`/`TY-CSTR` + keyword lexing; byte-identical-additive) → **S1** (`Char` UTF-8 lib + `DecodeResult` + ASCII classification) → **S2** (`ByteStr`/`Str` protocols + the four `deferror`s) → **S3** (`StrView` byte/codepoint iterators + slicing read layer, shared by String) → **S4** (`String` owning type: validating constructors, mutation, `ByteStr`/`Str`/`Eq`/`Ord`/`Hash`/`Drop`) → **S5** (`split`/`lines`/`trim`, `FromStr`/`parse`) → **S6** (examples/tests/docs/progress); scoped to specific functions/sites with subagent assignments, landmines (keep `Char` distinct from `ui32`; read-only conformers never `Drop`; borrowed views alias the owner), keep-green/byte-identical gates, and definition of done. **Out of scope:** the string-literal switch, full Unicode/UTF-16/grapheme, `String`-as-`Coll`
- [stage11/cleanup.md](stage11/cleanup.md) — Stage 11 collections cleanup spec (source of truth): **(1)** colon-paren binding sugar `name:(ref (Vector T))` (reader-fuse a trailing-colon atom + adjacent paren into the existing list form); **(2)** keyword type `:foo` built on a **shared `StrView` substrate** (immutable length-prefixed UTF-8 byte slice reusing `lib/hash.nuc`'s FNV byte-fold) so M6 `String` reuses the same byte/eq/hash layer — examined overlap, keyword stays interned-immutable, `Char`/UTF-8 deferred to String; **(3)** multi-binding `let` already works (premise mistaken — flatten the example only); **(4)** generic iterators — basic single-param works, but phantom multi-param templates `(MapIter I F S E)` fail (`unknown type: S` / segfault): **4a** fix phantom/positional tyvar recovery (bug), **4b** associated types (design doc only)
- [stage11/cleanup-prompt.md](stage11/cleanup-prompt.md) — condensed, actionable implementation prompt for cleanup.md: required reading + delegation workflow + the boot-rebuild/ground-verify step, then items in cost order — **3** (flatten `iterator-test.nuc`) → **1** (colon-paren reader sugar) → **4a** (phantom-param tyvar-recovery fix) → **2** (keyword on the shared `lib/strview.nuc` substrate + intern pool + `Hash`/`Eq`) → **4b** (`design/stage11/assoc-types.md`, design only); landmines, keep-green/byte-identical gates, and definition of done
- [stage11/assoc-types.md](stage11/assoc-types.md) — **implemented (A0–A2, stage11-collections branch)** — associated types for protocols. **No new declaration syntax**: parametric protocols already shipped, and the conformance registry's one-per-`(type,proto)` dedup *is* associated-type coherence, so every parametric-protocol parameter is recoverable (no `(type …)` marker needed). The one new surface is generalizing the `&where` constraint `(Protocol Var)` to a protocol *application* `((Protocol Arg…) Var)` — the same spelling `extend` already uses — where each `Arg` is **recovered** if an unbound tyvar or **constrained** if concrete (e.g. `&where ((Iterator S) I) ((UnaryFn S E) F)`); no infix `=`, no `with` keyword. Changes: retain bound args in the `Conformance` record, `emit-extend`, `&where` parser + fixpoint determination check, `unify-tpat` fixpoint binding from conformance args. Subsumes the deferred parametric-protocol-`&where` frontier (Known limitation #3 resolved); migration from phantom-param verbose forms; out of scope (HKT, lambdas, multi-conformance). See §7 of the doc for implementation status and resolved open questions.
- [stage11/assoc-types-prompt.md](stage11/assoc-types-prompt.md) — condensed, actionable implementation prompt for assoc-types.md: required reading + delegation workflow + boot-rebuild/ground-verify step, then tasks in dependency order — **A0** (retain conformance args in the `Conformance` record; coherence check; `.nuch` round-trip) → **A1** (generalize the `&where` constraint parser to `((Protocol Arg…) Var)`; fixpoint determination check) → **A2** (dispatch-time fixpoint binding from conformance args in `unify-tpat`) → **A3** (examples/tests/docs/progress); landmines, byte-identical-bootstrap gates, definition of done
- [stage11/assoc-types-extend.md](stage11/assoc-types-extend.md) — **implemented (A4.0–A4.4, all done)** — extend-site associated-type recovery so generic combinators can *conform* to the protocol they implement and thus **compose**. A `&where` clause on `extend` runs A2's recovery fixpoint at struct stamp time, recording the per-instance `Conformance` with recovered args (`(MapIter IntRangeIter SqFn)` → `Conformance{Iterator,[i32]}`). `.nuch` round-trip of `&where`-bearing extends (A4.3). `lib/iterator.nuc` rewritten with generic `UnaryFn`/`FoldFn`/`MapIter`/`FilterIter`/`reduce`, retiring the `*I64` specializations (A4.4). 89 tests pass; byte-identical bootstrap. See §12 for the build-order record.
- [stage11/cleanup2.md](stage11/cleanup2.md) — second collections cleanup pass (partially complete). **Done:** C2.0 (generalize `get` dispatch — `emit-get-with-callee` split; `generic-resolve-nullable`), C2.2a (`get:(Maybe V)` on `Assoc`; `hmap-get` deleted), C2.3 (`Set.select` comment corrected to design choice), C2.4 (`design/stage11/collections.md` §Seq reconciled with shipped lazy-iterator reality), C2.5 (`Call`/`BinaryCall`/`IntIndexable` removed from `lib/seq.nuc`; `examples/callable.nuc` migrated to `UnaryFn`/`FoldFn`), C2.6 (phantom-tyvar-test.nuc framed as legacy regression test), C2.7+C2.8 (doc/comment rationale sweep + resolved-limitation close-out), A4.5 (`defn &where` compound `((Protocol Arg) Var)` constraint support; `examples/assoc-iter-return.nuc` prints `count=5`; see `stage11/assoc-types-extend.md` §14). **Ready (unblocked by A4.5, not yet implemented):** C2.1 (lift `iter` into `Coll`), C2.2b (`keys`/`vals` into `Assoc`), C2.2c (`Entry K V` + `HashMap`-as-`Coll`).
- [stage11/cleanup2-prompt.md](stage11/cleanup2-prompt.md) — condensed, actionable implementation prompt for cleanup2.md: required reading (incl. `assoc-types.md` + the `examples/assoc-iter-return.nuc` value-return spike) + delegation workflow + the boot-rebuild/ground-verify step (confirm the C2.0 `get`-dispatch blocker is real before changing the compiler), then items in sequence — **C2.7+C2.8** (zero-risk doc/comment/limitation sweep) → **C2.0** (`get`-dispatch compiler change, byte-identical no-op on current source) → **C2.2a** (`get:(Maybe V)` on `Assoc`, delete `hmap-get`) → **C2.1** (`iter` into `Coll` + `(Entry K V)` map element + `doseq`/`into` migration) → **C2.2b** (`keys`/`vals`) → **C2.5** (remove legacy protocols) → **C2.3/C2.4/C2.6** (judgement cleanups); scoped to specific functions (`emit-get-with-callee`, `generic-resolve`, the `Coll`/`Assoc` heads, `doseq`/`into`) with subagent assignments, landmines (the protocol-arity break across all conformers; C2.0 must leave the bootstrap IR diff empty), keep-green/byte-identical gates after each item, and definition of done
- [stage12/namespaces.md](stage12/namespaces.md) — Stage 12: modules and namespaces. `ns` (default `user`); public-by-default with `name-` private definers (not-importable + internal linkage for link-emitting forms; private types opaque-through-public-signature with an `import-use`-suppressed warning); the import forms `import` (prefix-qualified) / `import-use` (legacy flatten) / `import-only` / `unsafe-import-private`, `include` removed (C headers via string-path import, foreign symbols keep bare ir-names); `export` re-export; `set-ir-prefix`. Slash is a **resolver** concern (first interior slash divides; bare `/` stays division) since `/` is already a symbol char. Key property: the default `user` namespace has an empty ir-prefix, so all-`user` + `import-use` code emits bare names and stays byte-identical — mangling (`foo/bar` → `@foo__bar`, composing before method/instance mangling) only bites on a non-`user` namespace. Conformance/protocol facts stay global on fully-qualified identity. Ends with the compiler-core split
- [stage12/namespaces-prompt.md](stage12/namespaces-prompt.md) — condensed, actionable implementation prompt for namespaces.md: authoritative decisions + invariants (additive-at-IR-level like parametric structs; opt-in-then-flip à la Phase F to stay green), then tasks in dependency order — **N1** (namespace core: `g-current-ns`, `ns`, first-interior-slash resolution, fully-qualified storage keying with `user` bare) → **N2** (import forms additive, old `import`/`include` kept as aliases) → **N3** (private definers + internal linkage + opaque-type warning) → **N4** (IR mangling + `set-ir-prefix` + cross-namespace conformance, the first IR-diverging task) → **N5** (`export`) → **N6** (`.nuch` round-trip + `--emit-cheader`/`apropos`/`locate`/`doc`) → **N7** (rewrite the tree to the new forms, then flip `import` semantics + delete `include`) → **N8** (split the ~13.9K-line core one file at a time as byte-identical code motion) → **N9** (docs/examples/progress); scoped to specific functions/anchors (`Sym` name/ir-name split src:231, `g-globals`, `scope-define`, `emit-import`, `emit-defn`/`emit-defvar` linkage sites, `type-mangle-token`, the `emit-nuch-*` family) with subagent assignments and a build-test-runner gate after each
- [stage12/progress.md](stage12/progress.md) — Stage 12 N1–N9 detailed task table
- [stage13/lambda.md](stage13/lambda.md) — Stage 13 (implemented): lambdas and closures. **Four forms** split by capture mode — `fn` (no runtime capture → bare function pointer, C-callable, zero overhead; may use inlinable constants/globals only), `vfn` (clone-capture via a new `Clone` protocol, source always survives: POD = bitwise/carefree, `Drop` capture = deep copy so the closure is itself owning), `mfn` (move-capture, consumes the source and owns the resource — the form that exports an owned value out of a `with` scope), `cfn` (reference-capture, borrows with an allocator-backed env, escape-checked). All lower to an **anonymous struct + `invoke`** (callable-values), with function-protocol conformance derived structurally on demand (no fixed arity/`UnaryFn` obligation; recognized set `{UnaryFn, FoldFn}`). Generalizes the `with` escape analysis from owned heap resources to **all frame-local storage** — a pointer-provenance check over scope-id "regions" that leaves `let` drop-free and fixes the pre-existing `return &local` UAF; sequenced **before** the closure forms. New `Clone` protocol (automatic for `Drop`-free types, hand-written for owning types). Capturing closures are excluded from generated cheaders + warned; `fn` decays to a C function pointer. **Landed (L0–L9): 117 tests pass, byte-identical bootstrap** — see [stage13/progress.md](stage13/progress.md); three pre-existing compiler limitations cap owning-closure cases at IR-level-verified. Motivates the eventual compiler `map`/`reduce`/`filter` refactor on the zero-cost monomorphized path. Deferred: mixed per-variable capture (keyword-per-mode chosen over a capture list), auto-derived `clone`, self-reference/recursion, nested-region precision
- [stage13/lambda-prompt.md](stage13/lambda-prompt.md) — condensed, actionable implementation prompt for lambda.md: required reading + delegation workflow + a read-only ground-verify (**L0**) that maps the escape/`invoke`/conformance/struct-template/worklist/`Drop`-synthesis machinery, then tasks in dependency order — **L1** (generalize escape analysis to frame-local regions, the gating soundness fix; the one non-additive phase, run measure-then-flip à la Phase F) → **L2** (`Clone` protocol, auto for `Drop`-free types) → **L3** (`fn` + free-variable analysis + lambda lifting; first cut forbids enclosing-local capture) → **L4** (anon-struct env + `invoke` + `vfn` clone-capture, the core) → **L5** (`mfn` move-capture via the `move` sink) → **L6** (`cfn` reference-capture, allocator env + region inheritance) → **L7** (structural function-protocol conformance derivation so closures feed `map`/`reduce`/`filter`) → **L8** (cheader exclusion + warning) → **L9** (docs/examples/progress); scoped to specific functions with subagent assignments, the byte-identical-bootstrap + IR-legal-generated-name + zero-cost-`fn`/POD-`vfn` invariants, a build-test-runner gate after each, and out-of-scope items (the compiler combinator refactor, inlinable-const `fn` locals, auto-derived `clone`, mixed capture, recursion)
- [stage13/progress.md](stage13/progress.md) — Stage 13 detailed progress table (L0–L9): what landed per phase, the key functions/files, the decisions the implementation sharpened (recognized-set `{UnaryFn, FoldFn}` scoping for L7; `__vfn_env_` prefix for L8 cheader detection; direct env emission for L4; the `fn-force-generic-mangled` gotcha), and the three pre-existing compiler limitations that cap owning-closure cases at IR-level-verified
- [stage13/closure-enhancements.md](stage13/closure-enhancements.md) — Stage 13 follow-on (not started): **feature enhancements** that complete the closure feature, distinct from the dogfooding refactor. **CE-1** named closures (`let`/`with` env-type inference — the refactor enabler), **CE-2** mutable capture (`set!`/`inc!`/`dec!` on a captured name — an accidental rewrite gap blocking stateful closures + `cfn` write-back), **CE-3** owning-closure export (lift the by-value-struct-return ABI + `with`-drop `TY-STRUCT` arming so an `mfn` can be returned out of a `with`; carries the `with-drop`↔`type-conforms-drop` inconsistency note), **CE-4** storable closures (why a closure can't be put in a collection/struct field today — anonymous, per-site, variably-sized env type — and the options, recommending a type-erased `(BoxedFn (params…) ret)` fat-pointer+vtable as the future direction; design exploration only). Also records the `cfn`-of-pointer escape imprecision (deferred nested-region precision)
- [stage13/functional-refactor.md](stage13/functional-refactor.md) — Stage 13 follow-on (not started): the **compiler+library dogfooding refactor** to express loops via named closures, eager combinators, collections, and iterators. Depends on CE-1 (named closures); uses closures downward-only so it needs neither CE-3 nor CE-4. Phases **R1** (eager combinator library + `ListIter`/byte iterators) → **R2** (raw-array registry → `Vector` migration, ~17 registries) → **R3** (compiler loop refactor, ~280 sites across find/any?/reduce/map/join/for-each/counted clusters, with a leave-alone list for ABI/parser/fixpoint/match-lowering hot paths) → **R4** (lib/examples refactor); byte-identical where inert (R1/R4), controlled reconverging refresh where structural (R2/R3)
- [stage13/type-erasure.md](stage13/type-erasure.md) — Stage 13 follow-on (**implemented, TE-0 … TE-7 done, 129 tests, byte-identical bootstrap**): the **shared type-erasure machine** — one two-word fat pointer `{ data, vtable }` with a **static per-concrete-type vtable** `{ invoke, drop }` — that realizes CE-4's `BoxedFn` *and* the deferred Stage 9 rung-5 `(dyn Protocol)` as **two instantiations of the same mechanism**. **Instantiation A — `BoxedFn` (`(BoxedFn (params…) ret)`):** spellable owning closure handle; erases the concrete env; enables `(Vector (BoxedFn …))`, a `BoxedFn` struct field, and a `(BoxedFn …)` return (closes the CE-4 env-naming gap). Bare `fn` boxes via a synthesized forwarder thunk. **Instantiation B — `(dyn P)` for a user protocol:** nominal (`extend`) conformance; enables B2 unbound-abstract returns (§8.5) + heterogeneous collections; method calls dispatch through the vtable. **Supersedes** the CE-4 Option 3 sketch and the rung-5 deferral. Zero-cost monomorphized path strictly preserved (type erasure is opt-in). v1 limits: single-method protocols only for `(dyn P)`; move-only boxes; process-default allocator; `BoxedFn`/`(dyn P)` public defns excluded from `--emit-cheader`. User docs: `docs/generics.md` §Type erasure
- [stage14/type-safety.md](stage14/type-safety.md) — Stage 14 (planned): retiring the compiler's untyped-pointer legacy. The internals still carry ~3,674 `(cast …)` forms re-attaching statically-known types to elem-less `ptr` fields/params/returns. This stage element-types the 26 registry `Vector`s (`(Vector ptr)` → `(Vector (ref StructDef))`), types the `compiler-types.nuc` cross-reference fields as `(raw T)`/`CStr`, types elem-less `:ptr` signatures, converts parallel-array-as-struct patterns (`Arm`/`Constraint`/`Field`) to typed Vectors, migrates the hand-rolled growables R2 left behind, and folds integer-tag closed sums (`Cleanup` pilot → `MethodKind`/`AbiInfo.kind`/`UnionDef.layout`; `Type`/`Node` kind-ladders → `case` **without relayout**) into `defunion`/`match`. The type-safety complement to Stage 13's loop-safety dogfooding (R2/R3 changed how the compiler *loops* and where its arrays *live*; Stage 14 changes what its records *are*). Phases T14.0 (ground-truth) → 14.1 registry element typing → 14.2 field typing → 14.3 param/return typing → 14.4 array-of-struct → 14.5 growables → 14.6 closed sums → 14.7 deferred-record; `(raw T)`-first (byte-identical, zero obligations) with selective `(ref T)` promotion, controlled reconverging refresh where structural. **Ground-verified correction:** the "prelude types can't appear in `nucleusc.nuc` signatures" constraint is stale (`prescan-imported-types` fixed it — a toplevel `Node`-typed signature compiles), so idiom-6 signature typing is fully reachable with no enabling fix; the real friction is the Phase-F `ptr:T`=`PTR-REF` semantics. Keeps the Stage 13 zero-cost hot-path invariant (no `(dyn P)`/`BoxedFn` on prescan/JIT/emit). Deferred: `Type`/`Node` as `defunion`, `Node.car/cdr` promotion, out-param boxes, HashMap side indexes
- [stage14/defn-signature.md](stage14/defn-signature.md) — Stage 14 (planned): move the `defn` return type after the parameter list — `(defn foo:ret (params) …)` → `(defn foo (params):ret …)` — matching the anonymous-function forms (`fn`/`vfn`/`mfn`/`cfn`). Syntax-only; **zero reader changes** (the `):ret` keyword-atom mechanics `fn` uses already exist; parenthesized return types stay space-separated per the colon-paren caveat). Inventories all ~13 read sites and 5 compiler-internal synthesis sites of the `name:ret` convention and funnels them through one new accessor (`defn-parse-sig`, returning the ret *node* for tyvar-safe template registration). Scope: `defn`/`defn-`, `defprotocol` method sigs, `declare`, `.nuch` `defmethod`/`declare`; explicitly not `defvar`/`let`/field bindings. Phases **S1** (dual acceptance, no rewrites) → **S2** (boot refresh) → **S3** (paren-aware mechanical rewrite of ~1,150 forms across src/lib/tests/examples, verified by emitted-IR identity diff à la the CStr migration) → **S4** (legacy style becomes a targeted hard error; docs sweep). Fiddliest consumer: generic-template stamping/tyvar substitution (ret tyvar moves into a `NODE-KEYWORD` spelling). Sequence **before** type-safety phase 14.3, which edits the same signature lines
- [stage14/macro-conditional-casts.md](stage14/macro-conditional-casts.md) — Stage 14 (planned): remove the cast requirement in macros that branch on argument shape. Ground-verified causal chain: macro params are already `(raw Node)` (every `(cast ptr:Node args)` in `lib/macros.nuc` is **vestigial** — verified removable today), but quasiquote/`gensym` results are elem-less bare `ptr`, `type-eq` keeps elem-less distinct from typed, and the three duplicated value-join sites (`emit-cond`, union `match`, niche-match arm) collapse mixed branches to void — so a whole-body `cond` macro dies with `returned null` unless the arg branch is `(cast ptr …)`. Fix: **MC-1** factor the joins into a shared `type-join` that **absorbs** an elem-less side to the typed side's elem at pkind `raw` (join-site analogue of the Phase-F `void*` flow exemption; join must not manufacture a non-null claim — qq can be null) → **MC-2** retype quasiquote + `gensym` results `(raw Node)` (requires MC-1 or the `null`-branch mix regresses; quote-atoms included but scoped out if implementation bites) → **MC-3** delete the casts in `lib/macros.nuc` + castless test → **MC-4** docs (macros.md sharp-edge rewrite; also fixes its pkind-must-match overstatement — kinds meet, only elems collapse). Rejected: wildcard `type-eq` (would merge `(f ptr)`/`(f ptr:Node)` overload identity), macro-return special-case (symptom only), `?Node` qq (narrowing-hostile). Expected near-byte-identical; standard reconverging refresh if statement-position mixed conds gain dead phis. Sequence before type-safety 14.3 (signature retyping enlarges the mixed-join class)
- [stage14/staging.md](stage14/staging.md) — Stage 14 cross-doc staging order: serial backbone **CP → MC → S (defn-signature) → T (type-safety)** driven by the hard edges CP-2→S1 and {MC-1, S}→14.3, with **AVR as a parallel track** (AVR-0 container toolchain immediately; refreshes never overlap; S3's tree-wide rewrite gets a quiet-tree window)
- [stage14/avr-targets.md](stage14/avr-targets.md) — Stage 14 (planned): AVR microcontroller targets (cross-compile only; reference devices ATtiny1634 = classic/Harvard-constrained, AVR32DD20 = modern mapped-flash via family `-mcpu=avrxmega3`). Ground-verified: the container LLVM 19 already has the AVR backend and emits valid AVR objects; functions live in addrspace(1) (function-*values* are a verifier error → v1 diagnostic); wide math/floats need avr-gcc's libgcc at link. Blockers mapped to phases **AVR-0** (container toolchain + spike) → **AVR-1** (backend registration, `--mcpu=`, static reloc, datalayout-derived ptr-align) → **AVR-2** (16-bit correctness: `ptr-int-ir` ternary 2→`i16`, descriptor-parameterized `emit-qq-helpers` — also the 32-bit blockers) → **AVR-3** (avr-gcc link driver, `--mmcu=`/`--linker=`/`--link-arg=`) → **AVR-4** (volatile MMIO lib + hand-written device register files; `<avr/io.h>` is macro-only, unparseable) → **AVR-5** (`fn-attr` directive + `"signal"`/`"interrupt"` ISRs via strong `__vector_N`) → **AVR-6** (Harvard hazards: prog-as diagnostic, rodata placement, `defvar` const form, the mapped-flash niche-sentinel collision) → **AVR-7** (all-aggregates-`ABI-MEMORY` AVR ABI; `f64` error) → **AVR-8** (make avr-test + docs). Byte-identical on hosted targets throughout; emitted programs already reference zero runtime symbols unless used (no `import-only` prerequisite)
- [stage14/riscv-linux.md](stage14/riscv-linux.md) — Stage 14 (planned): Linux on RISC-V (riscv64, RV64GC/lp64d only). Two tiers: **A** cross-compile from x86_64 + full test suite under `qemu-user` (the deliverable), **B** native self-hosting (specced, hardware-gated). Ground-verified: RISCV backend already in the container LLVM; 8-byte pointers so all width machinery is already correct; the load-bearing finding is the **features cliff** — empty CPU/features (today's `LLVMCreateTargetMachine` calls) silently codegens bare RV64I with the soft-float `lp64` ABI (`__muldi3` libcalls, doubles in integer registers), incompatible with riscv64 glibc; fix = `generic-rv64`/`+m,+a,+f,+d,+c` per-triple defaults + a `"target-abi"="lp64d"` module flag (first module-flag emission in the compiler). Also found: `repl.nuc:597/690` hardcode `"x86_64-pc-linux-gnu"` (live bug for aarch64 hosts — fixed in RV-0) and there is no non-x86 Linux boot IR or bring-up recipe (RV-4 adds per-arch boot selection modeled on `windows-boot`). Phases **RV-0** (container + repl-triple fix) → **RV-1** (registration + features/ABI plumbing, shared with AVR-1) → **RV-2** (cross link + qemu test lanes) → **RV-3** (integer-convention aggregate ABI; FP-flattening deferred like aarch64 HFA) → **RV-4** (native bring-up: riscv boot IR, JIT verify) → **RV-5** (docs). Byte-identical on existing targets; joins AVR on the parallel hardware track (RV-0/1/2 first — CI-verifiable hosted target early)
- [stage14/riscv-fp-abi.md](stage14/riscv-fp-abi.md) — **RV-6**: closes the riscv64 lp64d hard-float struct-flattening gap RV-3 deferred, found by the first native `make abi-test` on real riscv64 hardware (`mixed_get` 8.5→7.0, `farr2_make` 1.50,2.25→2.25,0.00). Flattens an aggregate's scalar members (recursing structs and arrays; a union is never flattened) and applies the psABI's three rules — one FP real → an FPR, two FP reals → fa0/fa1, one FP + one integer either order → FPR+GPR — falling back to RV-3's integer convention otherwise. Mostly a classification change: COERCE1/COERCE2 already carry arbitrary IR type strings, but five COERCE2 sites hardcoded `getelementptr … i64 8` (the second SysV eightbyte) and a flattened member sits at its own offset, hence `AbiInfo.off1`. **RV-6a/b/c/d all done.** RV-6c added the register counting the rules are conditional on: `abi-classify` is a pure wrapper over `abi-classify-avail (t gpr fpr)`, `abi-classify-arg` consults and debits an ambient budget (`g-abi-gpr-left`/`g-abi-fpr-left`/`g-abi-varargs`), and every walk over a parameter or argument list must call `abi-args-begin` first — the invariant that replaces threading a parameter through eight callers. The variadic tail needed no separate path (an FP availability of 0 *is* "integer convention, no flattening"), and §4's table was corrected: an `sret` return spends a GPR before the first argument, as clang does. Verified against clang's own RISCVABIInfo across the §2 table plus FPR/GPR exhaustion, the sret debit and a variadic call — every classification decision agrees — and pinned permanently by `tests/fixtures/rv6-fp-abi.nuc` + `run_rv6_fp_abi`. **The execution gate remains hardware-only (§7).** Still open and out of scope: clang coerces an integer-convention aggregate to whole XLEN units (`i64`/`[N x i64]`) where `abi-eightbyte-ir` narrows to the exact width, so 4-, 12- and 16-byte integer aggregates differ in spelling (8 bytes agrees); untested in either direction.
- [stage14/int-widening.md](stage14/int-widening.md) — Stage 14 (planned): integer-literal widening — close the residual cast-requirement gaps. **Premise correction (ground-verified):** the reported sites (`(arena-alloc 16)`, `(char-at sv 1)`, `malloc`, `usize` lets, mixed `usize`±literal arithmetic, `case`, field stores) already compile bare — Stage 7's implicit-cast machinery landed (`safe-coerce-val`, tier-2 `arg-adapts` with a working ambiguity error, `binop-coerce`); ~991 `(cast <int-type> <literal>)` forms in the tree are mostly vestigial. The genuine gaps: **A** template-tier methods never adapt — `(conj v 3)` / `(v 0)` on `(Vector i64)` fail "no matching method" even for non-tyvar `i:usize` params (the stamped collections API, the real friction locus); **B** explicit `(return 0)` in an `:i64` fn emits `ret i32 0` → raw LLVM parse error; **C** silent wrong code — `(take64 5000000000)` runs and prints `705032704` (default-i32 literal emission + wrap; `(take8 300)` silently truncates; no range checks, no `strtoull`); **D** `node-type-call` lacks the widening tier (lockstep hole). Phases **LW-1** (template-tier adaptation; adaptation never binds a tyvar) + **LW-2** (mirror in `node-type-call`, atomic for lockstep) → **LW-3** (explicit-return coercion + source-level error) → **LW-4** (representability checks, wide emission of out-of-i32-range literals, lexer overflow/`strtoull`) → **LW-5** (vestigial-cast sweep, IR-identity-verified per file). Rejected: suffixes, `(i64 N)` constructors, Go-style untyped constants, narrowest-candidate preference. Sequence LW-1 before type-safety 14.3
- [stage14/symbol-mangling.md](stage14/symbol-mangling.md) — Stage 14 (planned): extend symbol mangling so user function names may contain `?`/`!` (parity with `set!`/`aset!`/`inc!` and lib's `empty?`). Ground-verified: plain `defn full?` emits `@full?` verbatim → raw LLVM parse error (no source diagnostic); overloads sanitize but **lossily** (`is-both?` → `@is_both_.pA` — `?`/`!`/`-` all collapse to `_`); the `?`-breaking solitary path (generics.nuc:354) **is** the documented single-conformer bug behind the `_StrMangleShim` workaround — one line, two bugs. **Design landmine:** `sanitize-for-ir`'s safe set excludes hyphens (plain defns work only by bypassing it), so the fix must be a targeted map, never a blanket re-route. Decision: mnemonic mapping `?`→`_QMARK`, `!`→`_BANG` (Clojure munge precedent) in a shared `ir-name-token` at the base-token layer beside the existing operator table (`op-name-token` — `_+`→`add` etc.), applied by solitary/overloaded/fallback/defvar/nuch/REPL paths alike. Phases **SM-1** (shared token + always-tokenize solitary + delete `_StrMangleShim`) → **SM-2** (REPL name-derivation alignment; may fix a suspected pre-existing hyphen-name redefinition bug) → **SM-3** (cheader/`sanitize-for-c` + `.nuch` name truth) → **SM-4** (defvar/`%struct` verbatim sinks) → **SM-5** (source-level diagnostic backstop, docs — deletes two stale build.md gotchas). Rejected: quoted `@"full?"` identifiers (verified viable but touch-every-print-site + C-undeclarable), blanket sanitize (hyphen catastrophe), diagnostic-only. Bootstrap byte-identical (no `?`/`!` names in src/)
- [stage14/unsafe-namespace.md](stage14/unsafe-namespace.md) — Stage 14 (planned): the `unsafe` namespace + the `as` form — realize Stage 10's deferred "`unsafe/`-named namespace" promise (safety.md §1, "arrives with namespaces"; Stage 12 delivered them) so `grep -rn 'unsafe/'` enumerates every contract-subverting site. **Ground-verified:** one unchecked `cast` ladder (`emit-cast` — no range/null check, and it **bypasses `pkind-flow-check`**, silently laundering `raw`→`ref`) serves ~5,340 tree-wide sites of which the large majority are safe ceremony; special-form dispatch is raw-head identity *before* `qualify-name`, so `unsafe/` is a **reserved pseudo-namespace** (one identity compare per op, `/` is a symbol char, no reader change, `(ns unsafe)` rejected); `as` is unclaimed; `safe-coerce-val`/`coerce-int-val` (which *does* honor the flow check) already implement the safe set. Design: split `cast` → **`as`** (exactly what implicit coercion accepts + pure pointer-contract *weakening* — refuses lossy/laundering/reinterpret with errors routing to `as-ref`/`unsafe/cast`) + **`unsafe/cast`** (today's cast verbatim, superset so migration never blocks); move the always-unsafe roster (`funcall-ptr-*`, `ptr+`, `unsafe-import-private`→`unsafe/import-private`); keep the name-greppable C-parity core (`deref`/`aref`/`aset!`/`alloca`/…) bare by criterion. Phases **UN-1** (`as` + node-type lockstep) → **UN-2** (`unsafe/` routing + reservation) → **UN-3** (boot refresh + Class-2 sweep) → **UN-4** (cast split sweep — **hard edge: after MC-3/LW-5/14.2-14.3** so it only touches survivors of the cast-deletion work) → **UN-5** (retire bare spellings as targeted hard errors + docs). Rejected: enforced `(unsafe …)` block (needs an effect-system transitivity decision; still deferred), dot spelling, import-gated availability, blanket-moving all raw ops. Deferred: `try-as` checked narrowing (pairs with LW-4), debug bounds-checked `aref`
- [stage14/attributes.md](stage14/attributes.md) — Stage 14 (planned): **declaration attributes** — the keyword-attribute slot, resolving the storage-class-specifier deferral (stage888) and the overview TODO. A leading-keyword position on declarations (`(defvar :volatile x:i32 0)`, `(let (:volatile x:i32 0) …)`, `(:volatile field:i32)` fields/params) backed by a fixed registry (unknown keyword = error; flag vs valued shapes). Attributes qualify the *declaration*, never type identity/dispatch/mangling. Committed: **`:volatile` migrates** off the postfix type-qualifier spelling (`(T volatile)`/`T:volatile` retired with targeted diagnostics); pointer-target volatility keeps one type-position spelling `(ptr :volatile T)` (the MMIO case — pointee volatility genuinely travels with the pointer); lowering unchanged (`type-with-volatile`), pure front-end, zero volatile spellings in src/lib so byte-identical throughout. Reserved: `:thread-local` (registry entry + targeted error; implementation blocked on a threading stage — producer/consumer TLS agreement through `extern`/`.nuch`/cheader is a silent-wrong-code class, and nothing can test it without threads). Sketched: `:static` function-scope statics, `:align`/`:section`/`:weak`, fn attributes (AVR-5 ISRs). Phases **AT-1** (slot + `:volatile` + reservation, dual-accept) → **AT-2** (example/doc migration, IR-identity-verified) → **AT-3** (retire old spellings). No backbone edges; land AT-1 before AVR-4 so device files use final spelling
- [stage14/target-typed-constructors.md](stage14/target-typed-constructors.md) — Stage 14 (planned): **target-typed generic constructors** — fix collection-construction ergonomics at the root. Today tyvars bind from argument types only, so a zero-arg `(vector-new)` cannot exist (rejected at definition: `type variable 'T' is not determined by any parameter`, generics.nuc:1121 — a deliberate deferred gate), forcing the receiver-init idiom (`(with ((v (ref (Vector i32))) (alloca (Vector i32))) (vector-init v) …)` — type spelled twice) and the compiler's cast-laundering `mkvec`/`mkhash`/`make-vec` macros. Design: a one-shot downward **expected-type ("want") channel** from declared positions (`let`/`with` init, `set!`, `return`, field stores) into generic resolution, filling only tyvars the args left unbound (never re-ranking candidates); consume-once at the first emitted call so arguments never inherit it. **Ground-verified (2026-07-06):** by-value Drop bindings from by-value constructors already work (`with-drop-method`'s CE-3 by-value arm — compiled repro drops correctly), but by-value use sites pay `(addr-of …)` per method call, so TC-3 adds binding-position **value→ref materialization** (declared `(ref S)` + by-value `S` init → alloca backing + ref bind; matches the compound-literal yields-pointer convention). Phases **TC-1** (want in `generic-resolve`, lift the 1121 gate) + **TC-2** (emit-side want positions + `node-type` mirror, atomic lockstep) → **TC-3** (materialization) → **TC-4** (stdlib `*-new`/`*-new-alloc`/`*-new-in` constructor families; retire `mkvec`/`mkhash`/`make-vec`; one reconverging refresh) → **TC-5** (generalize `union-target-rewrite` to all want positions — `(let (m:(Maybe i32) none))` — and distribute want through value-position control flow, riding MC-1's `type-join`). Rejected: library `new`/`Init`-protocol macros, `with-new` binding form, extending `make` to templates, auto-ref receivers, full bidirectional typing. Prerequisites MC-1/LW-1-2/SM all landed; TC-4 before type-safety 14.1 (soft edge, same construction sites)
- [future/native-io.md](future/native-io.md) — Future (post-Stage-14): native strings and IO — retiring C stdio as the compiler's default so `CStr` retreats to the FFI boundary (the long-term half of native-strings.md's rejected full-excision option). Four pillars: a single-method `Writer` sink protocol (`dyn`-compatible; `String`/`File`/`BufWriter`/`StdOut`/`StdErr` conformers, formalizing the existing `g-out`/`open_memstream` sink-swapping pattern) + a thin `Fmt` carrier; a `ToStr` protocol (mirrors `FromStr`; writes into the sink, no intermediate allocation); a Clojure-style `str`/`str-alloc` concatenation macro (per-arg monomorphized, arena-capable — replaces the `fmt-*` helper zoo and its arity-segfault trap); `print`/`println`/`eprintln` + `read-line`; fd-based `File` with `Drop`-closed handles and `BufWriter`. Library split `fmt`/`io`/`file`. Compiler-replacement inventory (emission, diagnostics, source loading, REPL; what stays `CStr` forever) with **emitted-output identity** + throughput parity as the gates (byte-identical bootstrap is impossible for this flip). Hard prerequisite: struct payloads through `!T`. `Writer` shape carries an Option A/B split on the `(dyn P)` expansion (A = single-method on today's `dyn`, flush concrete-only; B = `flush` joins the protocol; A→B is additive, no ordering edge). Non-goals: format-string DSL, locale/graphemes, touching the interned-`ptr` identity substrate
- [future/dyn-arbitrary-protocols.md](future/dyn-arbitrary-protocols.md) — Future: expand `(dyn P)` from Stage 13's v1 (single-method, non-parametric) to arbitrary protocols. Ground-verified lift points: the two `die-at` gates in `dyn-vtable-method-irname`; `dyn-method-slot` already returns sig indices; `subst-proto-sig-node` already takes the params/args pair the parametric case needs; `Conformance.args` (assoc-types A0) is the admission hook. Design: **append vtable layout** `{m0, drop, m1, …}` (existing 2-slot vtables and the shared `@__boxedfn_drop`/BoxedFn-invoke slots stay byte-identical; slot(i) = i==0?0:i+1); **per-method dyn-safety** (4 rules — `(ref Self)` receiver, no `Self` in non-receiver position, no extra tyvars, concrete after application-arg substitution) with null slots + call-site diagnostics instead of whole-protocol rejection; **parametric applications** `(dyn (Iterator i32))` with concrete-only args keyed/admitted by conformance-instance spelling; optional BoxedFn-machinery convergence tail. Phases DP-0…DP-4, all byte-identical-bootstrap (compiler boxes nothing). Out: multi-protocol boxes (spelling collides with application form), downcast/RTTI, super-protocol vtables (no formal `extends` exists). First client: native-io.md's Option B `Writer`
- [stage14/colon-paren-types.md](stage14/colon-paren-types.md) — Stage 14 (**done 2026-07-02**): close the colon-paren type-sugar gaps. **Premise correction (ground-verified):** the base `name:(Type …)` sugar shipped in Stage 11 (`fuse-colon-paren`) and works in *every* ordinary binding position — let/param/return-name/defvar/defstruct-field/defunion-arm/protocol-sig/with/lambda/cast, including `r:(Result i64 i32)`; the doc records the verified support matrix. The real gaps: **G1** chain+paren `v:ref:(Vector i32)` mis-fuses (interior colon stays in the *name* — silent wrong-name in `let`), **G2** lone `:` after `)` mis-fuses to an empty name (the "colon-paren caveat" defn-signature.md §3 designs around), **G3** whitespace near-miss gives remote confusing errors. Fixes, all in `fuse-colon-paren` + one diagnostic: **CP-1** chain fuse `name:k1:…:(T)` → `(name (k1 (… (T))))` (reader stays type-ignorant; parser rejects bad segments) → **CP-2** lone-colon fuse `:(T…)` → the paren form itself, making `(defn f (params):(Maybe i32))` ≡ space-separated — retires defn-signature's parenthesized-ret exception, sequence before/with its S1 → **CP-3** targeted trailing-colon-name diagnostic (adjacency stays required; quoted data safe). Additive: stage1==stage2 without re-baselining. Out of scope: quasiquote `~sym:(T)` (unquote wraps the atom first), `!`/`?`+paren, quoted-data fusing (pre-existing, to be documented)
- [stage15-stress-test/overview.md](stage15-stress-test/overview.md) — Stage 15 (**W1–W7 done; reopened 2026-08-01 with W8 + W9, both now done — W8's six steps G-0 through G-5 landed 2026-08-01/2026-08-02, `compiler-init` eliminated and the no-initializer flip closes nullability.md §1.5; W9 reconciled to twenty items found, two fixed, eighteen open**): **stress-test fixes** — deficiencies found by using Nucleus for a project other than its own compiler. **W8** — combined declaration and initialization, spec [global-init.md](global-init.md): eliminate `compiler-init` or reduce it to a few genuinely special cases, declare-and-initialize a non-nullable global in one operation, startup call zero-cost when unused; closes the half of W6 §1.5 that item parked, and subsumes both findings the Doom-port regression run produced (its G-0 is the `defconst`/`defenum` import-order finding, its G-5 the `(defvar- g:ptr:T null)` one). **G-0 done (2026-08-01)** — `prescan-value-names` extends W1a's whole-graph walk to `defvar`/`defconst`/`defenum` names; all four §2.5 probes now resolve in both import orders; a latent silent-wrong-answer bug fell out and was fixed (a `defconst-` referenced earlier in its own file had resolved to another file's public constant of the same spelling); W1d's cycle diagnostic narrowed to what still cannot cross a cycle (macros, `deferror` ids, `extern`); tests 328→347, `make bootstrap` reconverged after a proven-inert 48-line type-definition-order diff. **G-1 done (2026-08-02)** — folds arithmetic/bit ops, `(sizeof T)`, `(as T lit)` (including `(as CStr "…")`, superseding W5c's out-of-scope note for that spelling) and `(addr-of g)` into `defvar-init-ir` with no JIT; overflow/division-by-zero/shift-out-of-range are located compile-time errors, never silent wraps; the inherited `int-literal-fits`/`pkind-flow-check` checks still fire on folded values; tests 347→361, `make bootstrap` byte-identical on the first pass. **G-2 done (2026-08-02)** — an `(array T N)` type usable as both a `defvar` type and a `defstruct`/anonymous-struct field type (the same feature the port's §4.2 Major finding names), landing all five spec shapes: the bare type (`zeroinitializer`), a constant array aggregate, `(defvar g:ptr:T (array T lit…))` → `@g.data` + non-null `@g = global ptr @g.data`, and a constant struct literal; array values decay to `(ref T)` on read — C's model, forced by the C-interop invariant rather than chosen — and are refused everywhere a value copy would be implied, gated by one consumed-once permission (`g-array-ok`) rather than N per-position refusals; the `g-arena-alloc` conversion the spec's own *Verify* clause demanded was split out (it cannot coexist with byte-identical bootstrap, since the committed boot compiler cannot parse a constant struct initializer at all) and de-risked rather than merely deferred, reproduced end-to-end as future G-2b/G-5 work; struct packing stays deferred, now **measured**, not assumed, to need nothing from that design (all six layout shapes and four ABI shapes match the platform C compiler with natural alignment alone); tests 361→385 (`NUCLEUS_TEST_JOBS=1`), `make bootstrap` byte-identical on the first pass, `make abi-test`/`make layout-test` extended and green. **G-3 done (2026-08-02)** — a `defvar` initializer G-1/G-2 cannot fold is queued and drained at `emit-toplevel-forms` depth 1 into a synthesized `void @__nucleus_init()`, registered by `llvm.global_ctors` (hosted) or refused with a located error (`none`, AVR); the queue predicate is `defvar-init-ir`'s own answer via a one-shot `g-defvar-soft` flag, not a second classifier; zero-cost-when-unused is proven by a two-sided tripwire (a constant-only unit emits neither artifact; the same unit plus one runtime initializer emits both); a `.nuch`+`.o` library with no Nucleus `main` anywhere initializes correctly, the case a synthesized entry point cannot serve; a REPL `import-use` gap in the spec's own §4.5 (ORC has no initializer entry point) was found and closed; tests 385→393. **An interlude (2026-08-02)** fixed a G-0 regression — `(defvar g:(fn i32)(i32) null)` collided with itself because `name-existing-kind` classified any `TY-FN`-typed global `Sym` as a function — with an `(= (sym is-local) 0)` conjunct, the same test `emit-dispatch` already used; unblocked G-5, which needs two fn-pointer hooks; tests 393→396. **G-4 done (2026-08-02)** — a `defvar` initializer that syntactically names a global whose own `defvar` has not been reached is a located error naming both sites, via a `defvar-state` field on `Sym` (DECLARED at G-0's prescan, REACHED at emission) rather than a second registry, since a saved `Sym*` would go stale across a scope-array growth; `(addr-of g)` is exempted as not-a-read. **G-5 done (2026-08-02) — acceptance criteria (A) and (B) both met.** `compiler-init` goes from 52 statements to zero (48 globals converted, 20 dead statements deleted, seven helpers removed); the sole survivor is `(target-init)`, called directly from `main`/`repl-main`, because it reads argv and derives five globals from one call; the flip makes `(defvar g:ptr:T)` with no initializer a located error, closing [stage15-stress-test/nullability.md](stage15-stress-test/nullability.md) §1.5's remaining half; the warn stage measured the flip's cost first — 54 sites, all in `src/`, zero in `lib/`/`examples/`/`tests/fixtures/`; a prerequisite defect (`as` not arming the want channel for a return-only-tyvar generic in `scope-new`) was found and fixed at its root; bootstrap deliberately moved and was reconverged via a two-hop fixed-point proof (C0→C1, C1 self-compiles byte-identically, then the standard reconverge), since the committed `bin/nucleusc` predates G-1/G-3. Tests 396→406 (G-4), 406→**410** (G-5). `make test` **410/410**, `make bootstrap` reconverged, `make abi-test`/`make layout-test`/`make avr-test` green. **W9, reconciled at stage close** — twenty pre-existing defects found across the whole W8 effort, two now fixed (the fn-pointer collision above, and `as` not arming the want channel above), **eighteen open**: the original six [global-init.md](global-init.md) §7 reports (`make lib-objs`/`make lib-so` broken on the committed boot; two separately compiled Nucleus objects unlinkable; `--emit-cheader` exports no globals; `--emit-cheader` emits hyphenated invalid C identifiers — a missed `sanitize-for-c` call site, which reaches type names but not field or function names; `(exclude-prelude)` in an imported file dies `unknown top-level form`; and the misleading `not defined anywhere in this compilation unit` message, now **closed at the cause by G-0** for the import shapes it covers) plus four more found measuring G-1, recorded in [stage15-stress-test/progress.md](stage15-stress-test/progress.md) rather than in `global-init.md` §7: `pkind-flow-check`'s `CStr` carve-out accepts a null through a non-null `(ref T)` (in both a `defvar` initializer and the identical local); `emit-as` ignores a literal's value when checking int-narrowing, so `(as i8 5)` is refused though it fits; `(defvar g:i1 5)` emits `global i1 5`, silently truncated to `true` by LLVM; and `tests/run-tests.sh`'s parallel unit output can interleave, making its PASS count unreliable by ±1 (FAIL detection unaffected); plus two more found building G-2, also recorded as `global-init.md` §7's own #7/#8 — three `fmt-s` call sites passing two substitutions to a one-argument helper, a cold diagnostic path that segfaults the compiler with no output if ever hit, and a wrong-arity call to a solitary (non-overloaded) `defn` going entirely undiagnosed (very likely why the `fmt-s` sites above were never hit); and one more found closing out this stage record — `parse-type-from-node`'s `unable to parse type expression` diagnostic is unreachable for an unrecognized type-expression head, surfacing instead as a misleading `missing :type`. Reconciling against `global-init.md` §7 (which grew independently while building G-3 and G-5) folds in four more, held open: `aref` emits a hardcoded `i64` GEP index, invalid IR on AVR; a `defvar` may be declared twice in one unit with no diagnostic; `(= h null)` does not compile for a fn-pointer value (`emit-binop-vals`'s null-literal escape and pointer-comparison arm both gate on `is-ptr-like`, which excludes `TY-FN`); and `type-size` has no `TY-FN` case, so a fn-pointer slot emits `align 1` (conservative, not a miscompile) while `coerce-int-val` has no `raw`→`TY-FN` case, so `(let (f:(fn i32)(i32) null) …)` still dies though the identical `defvar` now compiles — the last three were never filed in either defect list before this reconciliation, documented only inline in `examples/fnptr-global.nuc`'s comments. [stage15-stress-test/progress.md](stage15-stress-test/progress.md)'s W9 table is the full twenty-item reconciled account. The W1–W7 record below is unchanged. Input is a ~25,000-line Doom port (`/home/zak/code/nuc-doom-claude`, phases 0–5 complete, bit-exact demo replay) and its curated findings report `NUCLEUS-FINDINGS.md`. **Seven** work items, each with a spec doc, dispatched from [stage15-stress-test/prompt.md](stage15-stress-test/prompt.md): **W1** [resolution.md](stage15-stress-test/resolution.md) — whole-unit signature resolution, retiring the ordinal rule *"X may reference Y ⟺ Y begins processing before X is emitted"* by extending `prescan-imported-types`' existing depth-first import walk to protocols+signatures (plus per-file `ns` correctness, which the walk lacks today) — **done** (W1a–c landed 2026-07-31). **W1d** (mutual-import policy) was decided twice the same day: first **Option 1** (keep `circular import` a hard error, now-correct advice since W1a made "a common parent imports both" the recommended, order-independent spelling), then **superseded by the user's choice of Option 2** — import cycles are legal, emitted idempotently per path (each file emits once, at first reach), with four emission-time couplings (a partner's `defmacro`, a partner's `defconst`/`defenum` member, a partner's struct/union **layout**, a `prefix/name` alias over a cycle member) diagnosed with a located, cycle-naming error rather than supported, since supporting them needs a macro/layout prescan out of this stage's scope. **W1e** (`declare` as a forward prototype) resolved by obsolescence — W1a's reachability rule removed the only reason the port wanted it. **W2** [literal-typing.md](stage15-stress-test/literal-typing.md) — one root cause, `node-type-call` returning *operand 1's* type for intrinsic binops while emit unifies via `binop-coerce`, producing both invalid IR (`sext i32→i64` on an already-`i64` value) and spurious mixed-sign errors that reordering `(* 2 x)`→`(* x 2)` "fixes"; **W3** [cheader.md](stage15-stress-test/cheader.md) — C header interop, which was unusable for real libraries (opaque forward decls skipped at cheader.nuc:558, intrinsics headers emitted as invalid IR, `off_t` misparsed to `ptr` silently overriding a hand-`declare`) — **done** (W3a–c plus the `declare`-parameter fallout; opaque handles register layout-less, C type-qualifier positions and a validity gate are correct, typedef chains resolve transitively; `SDL2/SDL.h` imports, links and runs with zero warnings); **W4** [diagnostics.md](stage15-stress-test/diagnostics.md) — the line-0 error class and silent non-registration (`(defconst K:i32 2)` registers nothing with no diagnostic), **sequenced first because it makes every other item debuggable**; **W5** [ergonomics.md](stage15-stress-test/ergonomics.md) — the union-with-function-pointer **segfault** (confirmed live), `defn-` having linkage but no name isolation, `bit-not`, `\x` escapes, array-literal warts — **all done**: the union crash turned out to be a colon-paren *reader* gap (a function-pointer type is two paren groups and the fuse absorbed only one, leaving a stray `()` that ten sites dereferenced as NULL), not a union bug; `defn-` isolation shipped as **Option 1, unconditional** — a private definer with no `(ns …)` is keyed under an implicit per-file namespace (`#p1/helper`), decided by census (`src/` uses zero private definers, so no private-definer change could move the bootstrap, removing the reason to hedge toward the cheaper collision-only variant); **W6** [nullability.md](stage15-stress-test/nullability.md) — nullability flow typing, **design only** and **written** — re-probing corrected four of the item's premises: flow narrowing *already exists* (Stage 10, `nucleusc.nuc:1469-1660`) and is merely gated to `?T` bindings referenced by bare name, so §3.3 is really three things (`raw` never narrows; a **field read** is not a narrowable place; and `as-ref`+`when-some` already solves most of it but was used **0** times against **2113** `unsafe/cast`s in the port). Defines a two-form place language (`P-LOCAL`, `P-FIELD`) with stability conditions (no call, no store, no loop entry, no `label`, no `set!`, no address-take, never a global), rejects the desugar-to-`if-some` route as circular (`if-some` is already built *on* the narrowing engine, and never reaches `match` for pointers, so the niche-encoding blocker does not apply), specifies seven "cannot narrow here" diagnostics, and works three real Doom sites plus four rejections. Reports two **live soundness bugs** found while measuring — a `?T` **global** narrows across a call that nulls it, and an **address-taken** local narrows across a write through its address; both segfault today, both are zero-blast-radius fixes. Triages §3.4 as **not part of this feature and pointing the wrong way**: the local is explicitly declared (not inferred), the failure is the `null` *initializer* not a later field write, W4 already fixed the location, and the global that "allows" it (`(defvar g:ptr:T null)`) compiles to a null in a non-null slot and segfaults — a hole to close after W5c, not a permission to extend to locals; **closed 2026-07-31** (see [nullability.md](stage15-stress-test/nullability.md) §1.5) — `defvar-init-ir`'s `null` branch now calls the *same* `pkind-flow-check` the local path calls, so `ptr:T`/`(ref T)` reject `null` and `CStr`/elem-less `ptr`/`(raw T)`/`?T` stay legal with zero drift between the two paths. **Left deliberately open**: `emit-defvar`'s no-*initializer* default (`(defvar g:ptr:Thing)`, with no `null` at all) emits the identical unsound `global ptr null` for any `is-ptr-like` type — 53 such globals exist in the compiler's own source alone, all process-lifetime singletons filled after definition — so an author blocked by the new rejection can get the old unsound behavior back by deleting the word `null`; closing that is a deferred-initialization language question for W6 proper, not a check, and the external Doom port hit exactly this asymmetry 13 times (see below). Flow typing itself (§4 onward) remains **design only** — no `src/` change beyond the §3.4 triage item. Ordering W4→W2→W3→W5→W1, cheapest-and-most-isolated first with W1 last as the only item that changes *when* things happen. Every item is expected bootstrap-byte-identical (the compiler's own source compiles today, so these fixes touch only programs that previously errored). Stage success is measured **externally**: the Doom port rebuilds with its load-bearing import ordering and `(as ui32 CONST)` casts deleted, demo gates still bit-exact — **met 2026-08-01**: `test_demo` (182 checks, 35 tics) and `test_demo_monsters` (1654 checks, 150 tics, with monsters) both bit-exact against the new compiler with every listed workaround removed. Two findings came out of that run, recorded as top candidates for the next stage alongside struct packing/fixed-size array fields: the W6 §3.4 fix immediately broke 13 lazily-initialized `(defvar- g:ptr:T null)` singletons in the port (fixed by deleting the redundant ` null`, and direct evidence the no-init-spelling asymmetry left open above matters in real code), and `defconst`/`defenum` members are still import-order dependent — W1 covered `defn` signatures and protocols only, and the failing order's diagnostic falsely claims the name is "not defined anywhere in this compilation unit" when it has merely not been processed yet. **W7** [selector-ambiguity.md](stage15-stress-test/selector-ambiguity.md) — **done** (provenance: the author's own stress testing of the language, not the Doom port — no `§` number) — a bare symbol in selector position is *always* a field name, so `(m k)` and `(get m k)` on a `HashMap` die with `no field 'k' on struct 'HashMap.cstr.i32'` while `(m "foo")` works; `selector-literal-sym` classifies by node kind alone and `emit-get-with-callee` has **no edge from its symbol branch back to its value branch**, and `(invoke m k)` cannot serve as the escape hatch because `emit-invoke-with-callee` never falls back to `get`. Blast radii **measured** by instrumenting the emitter and compiling the compiler plus every example: scope-first resolution would silently re-interpret **79** sites in `src/` (`name` x27, `line` x17, ...) and turn `examples/get-dispatch-test.nuc`'s `(= (self key) key)` into infinite recursion, so it is rejected; the `invoke`-symmetric rule (a value-keyed-`get` type gives up callable field access, as `invoke` types already do) costs **26** distinct lines, 23 of them mechanical `_get` conversions inside `lib/hashmap.nuc` — the stdlib's **only** value-keyed `get`. Recommends that rule plus an `invoke`->`get` fallback (which gives the language its missing unambiguous always-a-value spelling, pairing with `'sym` as always-a-name), and a provably **zero**-blast-radius "field-first, value fallback" variant, as the in-stage fix. Also evaluates **marked selectors** — a field name must be written `'field`/`:field`, freeing bare symbols to mean variables as they do everywhere else, **selector position being the only place in the language where a bare symbol is not a variable reference**: measured at **~3,800** sites (4,151 emit-time occurrences compiling the compiler) of which **0** are currently quoted, against **49** static `_get` uses. Both marks already exist in the reader (`'sym` is accepted by `selector-literal-sym` today; `:foo` lexes to a `NODE-KEYWORD` the compiler already treats as a compile-time name for type spellings and declaration attributes, and is unused in selector position), and the rewrite is automatable from `g-source-path` locations via a `--warn-bare-selector` mode, so the doc stages it accept-both -> warn-and-rewrite -> flip, the same shape as the Stage 10 Phase F nullability flip. **`:field` is rejected**: keywords are designed as HashMap keys (`lib/keyword.nuc`'s stated purpose), so `(m :name)` would be irreducibly ambiguous between the field `name` and the keyword key `:name` — the collision landing exactly on the type that motivated keywords; `'field` carries the same shape of collision only against a *symbol*-keyed map, judged rare enough to answer with "define your own `get`". Concludes marked selectors are the **correct end state** — the other options are tiebreak rules for an ambiguity it deletes — while noting it reverses Stage 9's deliberate migration *to* head-position field access, and defers it to its own stage. **Shipped in W7 instead:** the bare symbol demotes to a value selector when the callee provably has no such field and the name is a **local** (locals only — every function is in the global scope, so demoting on globals would re-interpret `(sd name)`); `invoke` falls back to `get`, making it the always-a-value spelling paired with `'sym` as always-a-name; and the "no field" diagnostic names the shadowing local. Zero blast radius **measured**, not argued — A/B-diffing emitted IR for every example against the pre-change compiler gives 135 byte-identical, 0 differing, 1 newly compiling; `make bootstrap` holds; 297→300 tests. Out of scope: libclang rewrite, struct packing/fixed-size array fields (deferred — they want the [stage14/attributes.md](stage14/attributes.md) layout-attribute design)
- [global-init.md](global-init.md) — **design (2026-08-01, revised the same day after four rulings); filed as Stage 15 W8 — DONE, all six steps G-0 through G-5 landed 2026-08-01/2026-08-02 (plus one regression-fix interlude between G-3 and G-4); acceptance criteria (A) `compiler-init` eliminated and (B) the no-initializer flip are both met** — combining declaration with initialization: `defvar` can only define statically, so every process-lifetime global must be declared in one place and filled in another, which is both unergonomic and the reason [stage15-stress-test/nullability.md](stage15-stress-test/nullability.md) §1.5 had to park the *no-initializer* half of the §3.4 hole (`(defvar g:ptr:T)` still emits the unsound `global ptr null` and segfaults; only the explicit ` null` spelling was closed). **Separates three conflated problems** — (1a) a global whose initializer is a *runtime* expression, (1b) one whose value is a compile-time *constant* the renderer merely cannot express, and (1c) ordering/use-before-initialize — and shows 1b raises none of 1c. **Four corrections to its own brief, each measured:** the "whole program is one translation unit" premise is **false** for the `--emit-nuch` + `.o` library route (verified: a consumer sees `@gb = external global ptr` and links against a separate module, so an initializer list reachable only from the consumer's `main` cannot initialize a library); **`llvm.global_ctors` is silently a no-op on AVR** (the entry type is invalid without `ptr addrspace(1)`, and even then LLVM's `.init_array` lands at VMA `0x800100` in RAM while avr-libc's `__do_global_ctors` walks `__ctors_start..__ctors_end`, which the linker leaves **empty** — `0x68 == 0x68` against an avr-gcc reference build that differs by one entry); the cost framing is **95% off-target**, since **20 of the Doom port's 21** hand-rolled `ensure-*` lazy initializers are 9 constant tables plus 11 fixed-size zero-filled buffers and need *no runtime initialization at all* (only 1 is genuinely runtime), so what they want is C's `T name[N] = {…};`; and the compiler census is **174** `defvar` forms (not 254), of which 114 have no initializer and **53** are non-null element-typed — that half reproduces exactly. Adds the option the brief did not list and the measurements pick: **constant-fold in `defvar-init-ir` with no JIT**, plus an `(array T N)` **type** (the same feature the port reports separately as Major finding §4.2, "no fixed-size array struct fields"), emitting `@g.data = internal global [N x T] […]` + `@g = global ptr @g.data` — verified legal, **provably non-null**, and *negative* cost: a faithful 137×-3-field reproduction of `mobjinfo` measures **1401 IR lines / 139 allocas / 550 runtime stores** for the current workaround versus **0** for a constant aggregate. Rejects **compiler-generated lazy init** (640 static reference sites in the compiler alone; changes `emit-symbol-ref`, the one emitter that cannot be handed a node; and makes non-null a *flow* fact, i.e. needs the W6 machinery it was supposed to avoid) and **JIT-folding** (measured blocker: a CT module can call neither a program `defvar` — `use of undefined value '@gz'` — nor a program `defn` — `Symbols not found: [ bump ]` — which also makes the whole JIT-tier question **moot**, since those modules have no program globals to initialize; plus it is cross-compilation-unsound and can never yield a runtime address). Prices the do-nothing alternative exactly: re-spelling the compiler's 53 globals `raw:T` costs **249 flow violations at 197 distinct lines across 10 files** (184 argument, 27 assignment, 26 return, 12 `as`; control run = 0), measured by building a warn-only compiler in scratch — ~200 new `unsafe/cast`s in the compiler's own source, the wrong direction for a stage whose headline defect is 2113 of them. **Revised 2026-08-01 against four rulings, which changed several of its conclusions.** **(i) The headline goal is now `compiler-init`, not the port** — eliminate it or reduce it to a few genuinely special cases, with globals that should not be nullable declared and initialized in one operation; the `(defvar g:ptr:T)` rejection stays as the derived criterion. The new **§2.12** is the analysis this turns on and is now the most important measurement in the document: `compiler-init` is **51 statements** and runs **exactly once per process**, of which **30 become combined declaration+initialization covering 48 globals** (3 constant, **45 runtime**), **20 are dead** (they restate the `defvar` zero default; no per-unit reset is load-bearing), and **1 is the residue** — `(target-init)`, which reads argv and so can never be an initializer, plus it derives five globals from one call. It also found a *third* spelling of the same unsoundness: `g-vtable-table`/`g-boxedfn-table`/`g-dyn-table` are declared non-null and **never initialized at all**, built lazily behind a `= null` guard. **(ii) Ordering is accepted policy**, with reordering the compiler's own source sanctioned where that is what makes an initializer expressible (two known reorders, both trivial); the resolution-vs-initialization distinction W1 drew is kept and belongs in `docs/`. **(iii) Zero-cost-when-unused is a hard requirement** (microcontroller binary size): a program with no runtime initializer emits **nothing**. The determination is "is the queue non-empty?", it is **exact**, and it is known at exactly one point — `emit-toplevel-forms` at `g-toplevel-depth == 1`, beside `drain-mono-worklist` — so every artefact of the runtime half is emitted there or nowhere. That requirement forces every mechanism to be **append-only at that point**, which **rules the synthesized-`main` wrapper out of v1** (the rename happens in `emit-defn`, long before the queue's emptiness is known) and thereby *simplifies* the per-triple split rather than complicating it: `ctor-mechanism-for-triple` returns `global_ctors` or `none`, and on a triple with no verified append-only mechanism a non-empty queue is a **located error**, never the dead `.init_array` section AVR silently produces today. **(iv) Do not convert the Doom port**; it stays as evidence (the 20-of-21 static split is real and still justifies shipping the static half first) but gates nothing, and G-2's "stop and re-measure" step is removed. **The revision also corrected two of its own first-draft conclusions**: the `g-arena-alloc` migration blocker **dissolves** — `AllocHandle` is `{kind:i32, data:ptr}` and the arena handle is `{ALLOC-ARENA, null}`, a compile-time **constant struct** reachable by the static half alone, with the arena lazily initialized by `arena-alloc` so there is no ordering constraint at all, and the constant form additionally *fixes* the live split where `add-include-path`/`add-link-arg` run before `compiler-init` and silently get libc-backed vectors; and Option 1 is retired from v1 as above. Adds **Option 7**, placeholder-then-substitute, as an explicitly-marked last-resort code smell (it converts a crash into a wrong answer) — **which no current site needs**, since `g-arena-alloc` did not require it. Prerequisite **G-0**: `defvar`/`defconst`/`defenum` names are *still* import-order dependent, same-file **and** cross-file (all four probes measured), with the misleading `not defined anywhere in this compilation unit` message — W1 covered only `defn` signatures, protocols and type names; G-0 extends that walk to value names, is shippable on its own, narrows the ordinal surface to initializer *sequencing* rather than resolution, and is the Doom-port regression run's second finding. Staged **G-0 (done) → G-1 (done) → G-2 (done — an `(array T N)` type + constant aggregate initializers) → G-3 (done — `@__nucleus_init`, emitted only when non-empty) → G-4 (done — the ordering diagnostic) → G-5 (done — `compiler-init` eliminated, then the flip)**, accept-both → migrate → warn → flip like Stage 10's Phase F — **all six steps landed**. **G-0** (`prescan-value-names`) extends the whole-graph prescan to `defvar`/`defconst`/`defenum` names, closing the ordinal surface for both plain and `import-use` symbol imports; tests 328→347, bootstrap reconverged after a proven-inert type-line-order diff. **G-1** folds arithmetic/bit ops, `sizeof`, `as`, and `addr-of` into `defvar-init-ir` with no JIT — including `(as CStr "…")`, which supersedes Stage 15 W5c's note that the general as-lit rule left that spelling out of scope (see [stage15-stress-test/overview.md](stage15-stress-test/overview.md)) — with overflow/division-by-zero/shift-range as located compile-time errors, never silent wraps; tests 347→361, bootstrap byte-identical on the first pass. **G-2** adds the `(array T N)` type itself — usable as both a `defvar` type and a `defstruct`/anonymous-struct field type, decaying to `(ref T)` on read (C's array-in-struct model, forced by the C-interop invariant) and refused everywhere a value copy would be implied, gated by one consumed-once permission (`g-array-ok`) rather than N per-position refusals; the spec's own *Verify* clause conflated the feature with converting `g-arena-alloc` to use it, which cannot coexist with byte-identical bootstrap, so that conversion is split out (de-risked, not merely deferred) as future G-2b/G-5 work; tests 361→385 (`NUCLEUS_TEST_JOBS=1`), bootstrap byte-identical on the first pass. **G-3** drains the fold-resistant queue into a synthesized `void @__nucleus_init()` at `emit-toplevel-forms` depth 1, registered by `llvm.global_ctors` on hosted triples and refused with a located error on AVR (`ctor-mechanism-for-triple` answers `none`); zero-cost-when-unused is proven by a two-sided tripwire, and a `.nuch`+`.o` library with no Nucleus `main` anywhere initializes correctly through it; tests 385→393. **An interlude** fixed a G-0 regression that made a function-pointer-typed `defvar` collide with itself (`name-existing-kind` classified any `TY-FN`-typed global as a function; fixed with an `is-local` conjunct); tests 393→396. **G-4** rejects a `defvar` initializer that syntactically names a global whose own `defvar` has not yet been reached, naming both sites, via a `defvar-state` field on `Sym` rather than a second registry (a saved `Sym*` would go stale across a scope-array growth); tests 396→406. **G-5** is acceptance criterion (A): `compiler-init` goes from 52 statements to zero (48 globals converted, 20 dead statements deleted, seven helpers removed, `(target-init)` the sole survivor because it reads argv), plus the flip — (B) — rejecting `(defvar g:ptr:T)` with no initializer, closing [stage15-stress-test/nullability.md](stage15-stress-test/nullability.md) §1.5's remaining half; the warn stage measured the flip's cost first (54 sites, all in `src/`, zero elsewhere); a prerequisite defect (`as` not arming the want channel) was found and fixed at its root; bootstrap deliberately moved and was reconverged via a two-hop fixed-point proof, since the committed `bin/nucleusc` predates G-1/G-3; tests 406→**410**. Full as-built accounts are in the document's own G-0 through G-5 subsections and are mirrored, not restated, in [stage15-stress-test/progress.md](stage15-stress-test/progress.md). **§8's settlement held through to G-5**: the runtime half had to be built, because the deciding corpus was the compiler's own source at **45 of 48 runtime**, not the port's 1-of-21. The one question §8 left open is still narrow and still open: whether a `section ".ctors"` global with avr-gcc's word relocation is walked by `__do_global_ctors`; `none` remains AVR's answer and no v1 step needed the other one. Also reports six pre-existing bugs found while measuring — **filed as Stage 15 W9** — including `make lib-objs`/`make lib-so` being **broken on the committed boot**, two separately compiled Nucleus objects being unlinkable (7 duplicate prelude globals), and `--emit-cheader` emitting hyphenated invalid C identifiers because `sanitize-for-c` reaches type names but **not** field names or function names (a missed call site, not a missing mechanism). **W9 has since grown to thirteen**: measuring G-1 surfaced three more (a `pkind-flow-check` carve-out that lets a `CStr` null flow into a non-null `(ref T)`; `emit-as` refusing an int literal that fits because it ignores the literal's value; `(defvar g:i1 5)` silently truncating to `true`), plus a `tests/run-tests.sh` parallel-output finding that makes its PASS count unreliable by ±1 — those four are recorded in [stage15-stress-test/progress.md](stage15-stress-test/progress.md) rather than folded back into this document's §7. **Building G-2 surfaced three more still**: this document's own §7 #7/#8 — three `fmt-s` call sites passing two substitutions to a one-argument helper (a cold diagnostic path that segfaults the compiler with no output if ever hit) and a wrong-arity call to a solitary `defn` going entirely undiagnosed (very likely why the `fmt-s` sites were never hit) — plus one found closing out that stage-record pass and recorded in [stage15-stress-test/progress.md](stage15-stress-test/progress.md) only: `parse-type-from-node`'s `unable to parse type expression` diagnostic is unreachable for an unrecognized type-expression head, surfacing instead as a misleading `missing :type`. **Building G-3 surfaced this document's own §7 #9 and #10**: the fn-pointer `defvar` collision (**since FIXED**, in the interlude) and `aref`'s hardcoded `i64` GEP index (open). **Building G-5 surfaced §7's #11 and #12**: a `defvar` may be declared twice with no diagnostic (open), and `as` not arming the want channel (**since FIXED**, in G-5 itself). Three more were never filed in either list, found alongside the interlude and documented only inline in `examples/fnptr-global.nuc`'s comments: `(= h null)` does not compile for a fn-pointer value, `type-size` has no `TY-FN` case (`align 1`, conservative not wrong), and `coerce-int-val` has no `raw`→`TY-FN` case (a `let` with the identical spelling still dies). **Reconciled total: twenty defects found across the whole W8 effort, two now fixed, eighteen open** — [stage15-stress-test/progress.md](stage15-stress-test/progress.md)'s W9 table is the full account.
- [stage15-stress-test/name-resolution.md](stage15-stress-test/name-resolution.md) — **investigation (2026-08-08), no code changed**: why `examples/w9-dyn-ns.nuc` reaches a protocol as `dp/Describe` when the consumer imported it under the prefix `dpx`, and whether one resolution path is feasible. **Measured, not inferred:** three incompatible key policies across eleven name-keyed registries (`g-globals` ns-qualified; `g-generics`/`g-structs`/`g-macros`/templates/enums bare; `g-protocols` ns-qualified with a bare fallback), and `import-prefixed` is not a resolution mechanism at all but a post-hoc copy of `g-globals` entries filtered on `is-local`/`ir-name` — two fields that mean something else, which is why `defvar`, `defconst` and `defenum` members get no prefixed spelling. Nine defects, each with a probe: a **bogus** qualifier resolves on a type (`zzz/Fox` compiles and runs); an **overloaded** function has no qualified spelling in any namespace; a prefix declared in one file is visible in every other; the defining namespace is always in scope whether or not the consumer asked for it; `name-existing-kind` and `emit-dispatch` enforce and consume one-symbol-one-kind in **different** priority orders. **Verdict:** the proposed shape is feasible and `name-existing-kind` is already the resolver in skeleton (promoted from a definition-time check to reference-time, returning payload as well as tag). Split it: the **front half** (spelling → canonical name, prefix as a *scoped* binding) is ~13 sites and fixes eight of nine; the **back half** (one binding table for eleven registries) is ~200 call sites and entangles *identity* with *resolution*. **Recommends Option B** — front half + a `src-ns` provenance field validated after the hit (the `Method.src-ns` precedent), keys stay bare, no IR moves; type identity (`a/Vector` vs `b/Vector`) stays a separate later decision. Near-zero bootstrap exposure: nothing in `src/` declares a namespace and only four demo libs do, so every changed path is inert under `user` + `import-use`. **§7's three questions were then ruled on the same day (§8):** two namespaces *may* both define `Vector`; `import-use` flattens and a collision is an error; a namespace is unreachable without an import. The first ruling pulled identity into scope — and **measuring it withdrew B3**: `type-eq` compares `StructDef` *pointers*, so a type name is only a registry key plus a mangling token, making namespaced types a **re-keying** (plus an ns-aware IR token composed exactly as `ns-ir-base` already does for functions) rather than an identity change, after which a bogus qualifier misses on its own and provenance fields are unnecessary. Two consequences worth carrying: the collision rule must be **per kind** — name-only for types/protocols/macros/values, but name-**and-signature** for functions, since name-only would make protocols and `import-use` mutually unusable — and generics should therefore stay bare-keyed with methods merged across flattened namespaces, recovering the qualified spelling by filtering on the `Method.src-ns` field W5e already added. `unsafe` stops being special: a built-in namespace implicitly bound as a *prefix* in every file, which drops the seven hard-coded `unsafe/*` special-form strings and makes bare `cast`/`ptr+` stop resolving for free (UN-5's intent, currently seven hard-coded errors). Revised staging **B0** fixtures → **B1** file-scoped import environment → **B2** canonicaliser (protocols first) → **B3′** re-key types + mangling token (wants [stage14/symbol-mangling.md](stage14/symbol-mangling.md) first or folded in) → **B4** collision policy → **B5** priority-order reconciliation. **A second ruling round (§11) then settled all three, with measurements.** Collision reporting is **eager** — measured cost across `src/`+`lib/` is exactly **one** casualty (67 public `defstruct`, 19 `defprotocol`, 181 `defvar`, 38 `defconst`: only `Node` is defined in two files), and it is a latent bug rather than sloppy hygiene: `lib/prelude.nuc` and `lib/list.nuc` declare `Node` with **different field types**, both are in every compiler build (`src/type-mangle.nuc` `import-use`s list), and `emit-defstruct` silently discards the second by an explicit "skip if already emitted" guard, so import order picks the layout. The measurement also forces a phrasing fix: a collision must be **two definitions of one name reaching one scope**, not "two flattened namespaces", or the `Node` case (both `user`) escapes. `import-only` does **not** bind the namespace qualifier (accepted; note it currently filters *nothing* — `emit-import-only` passes `prefix=null`, measured to still expose unlisted functions *and* types — so the rule governs a filter B-work must build, and it needs a targeted diagnostic or unlisted names degrade into W1c's "not defined anywhere" lie). `export` is **correct as shown** in `examples/export-test.nuc`, but the `g-globals`-only reach is a scope limit today and a **blocker after R1** (§11.6): `emit-export` is `scope-lookup g-globals` + `scope-define g-globals`, so a facade re-exporting a type or protocol dies `export: symbol not found` (measured, both kinds) — harmless only because bare-keyed types are globally reachable anyway, which R1 removes, at which point a facade would export functions whose signatures name a type the consumer cannot spell. Generalising `export` therefore moves into B3′, and is a good check on the resolver: one that cannot express "re-register this binding under another name" is not carrying enough information. **The duplicate `Node` is fixed, not worked around (§11.7)**: `lib/list.nuc`'s redeclaration is removed (the prelude's already won via the "already emitted" guard, and nothing imports `list` without the prelude) — `make bootstrap` reaches its fixed point, **429 tests pass, 0 fail**, and the change is proven **byte-for-byte inert on emitted IR** by diffing against a clean `HEAD` worktree built with the same boot compiler (`boot/nucleusc.ll` is stale at `HEAD` by ~27k lines and is not a valid baseline). `examples/w9-dyn-ns.nuc` becomes **invalid as written** and is B2's acceptance fixture. **A-vs-B remains open**, but the rulings narrow it and then argue for B: R1 already forces the type re-keying that distinguished the two, and R2's conclusion that generics stay bare-keyed (merged across namespaces, qualified spelling recovered via `Method.src-ns`) is a per-kind exception a single uniform binding table cannot avoid. **B0 landed 2026-08-08** — `tests/resolution-matrix.sh` + `tests/expected/resolution-matrix.baseline`, 43 cells, a *recorder* rather than a gate (later steps diff against it), plus three regression tests for the cells that are already correct; it also corrected §2's protocol row, which splits in two — `extend`/boxing with a bare name is `err` rather than "falls back", and a `(dyn …)` **annotation** validates nothing at all in any column, a tenth defect. **B1 landed 2026-08-08** — an import prefix is now **file-scoped**: `ImportBind` + `g-file-imports` (a `(Vector (ref ImportBind))` on the `NsPrefixEntry` idiom), saved/cleared/restored around every imported file exactly as `g-current-ns` is, recorded by `do-import` **before** all three of its early returns (prefix dedup, flatten dedup, W1d cycle skip — each leaves the import *declared here* while doing no further work, and the cycle case would otherwise let B1's diagnostic answer a question `cycle-prefix-message` already answers). Alias injection is left intact; what B1 adds is the gate `prefix-out-of-scope`, consulted by `scope-lookup` on the global scope only and short-circuiting on a null `g-import-prefixes`, so a unit with no prefixed import — every build of this compiler — is unchanged. Exactly **one** matrix cell moved (`xfile-prefix-leak zx/` `ok` → `err`) and the diagnostic deliberately does not degrade into W1c's "not defined anywhere in this compilation unit", which for a name that is in the unit and reachable would be a lie; it names the prefix, the file that *does* bind it, and the prefixes in scope here. `make bootstrap` at its fixed point, **434 tests pass, 0 fail**. Two notes for B2 (§9.1): the prefix→**namespace** half is not materialised (`ImportBind` carries `{prefix, path}` — a `path → ns` memo written where `do-import` finishes a fresh load is what B2 wants), and one imprecision is left in — a qualifier that is both a namespace and another file's prefix is refused in a third file that binds neither, which B2 removes by making the environment the only authority for both mechanisms. **B2a landed 2026-08-08 — the canonicaliser, with protocols cut over; this is the chunk that fixes the originally reported defect.** `resolve-spelling(spelling) → ref:NameRef` (`src/nucleusc.nuc`; `NameRef` + `NR-BARE`/`NR-QUALIFIED`/`NR-UNBOUND` in `src/compiler-types.nuc`) splits at the first interior slash and resolves the qualifier through `g-file-imports` **and nothing else**: a bound prefix names its library's namespace, an `import-use`d namespace may also be named by its own name (§8.3 row 1's second clause, R2's escape hatch), the file's own namespace and `user` are always legal, and anything else resolves to **nothing — even when the raw spelling is literally the registry key**, which is the fix. The prefix→namespace half B1 left open is `g-file-ns`, written by **`emit-ns`** rather than at the end of a load block: `emit-ns` is reached from every file entry and the W1a whole-graph prescan runs first, so every reachable file's namespace is recorded before the first import is processed and B1's hard cases (already-loaded, W1d cycle skip) need no handling at all. Exactly the four predicted matrix cells moved — `protocol-extend` and `protocol-dyn-box`, `zx/` err→ok and `zn/` ok→err — and nothing else; `examples/w9-dyn-ns.nuc` is rewritten to `dpx/`/`dpx2/` with its output unchanged, and `dp/Describe` is pinned as an error in both the `extend` and the box position. `make bootstrap` at its fixed point, **438 tests pass, 0 fail**, and the whole chunk is **byte-for-byte inert on emitted IR** for all 182 files in `examples/`+`lib/` (measured against a clean `HEAD` worktree build). **The load-bearing finding for the A-vs-B decision (§12.6):** the canonicaliser is genuinely shared (~200 of ~290 new lines mention no kind), but "cut a kind over" is dominated by splitting that kind's lookup into a **reference resolver** and a **key lookup** and then classifying every existing call site by which one it holds — because a canonical name read back from a `.nuch`, a `Constraint`, a super-protocol edge or a `(dyn P)` box is routinely read in a *different file* from the one that wrote it. That distinction is a property of the string's provenance, not of the registry's shape, so **Option A does not remove it**; A deletes only the ~20-line per-registry probe cascade. Two open items reported rather than forced: the tenth defect (a `(dyn P)` *annotation* validates nothing) is blocked on prescan ordering, not on the canonicaliser — `dyn-type` runs inside `prescan-defn-signatures`, which for the root file precedes `prescan-imported-signatures` and every import form, so a check there would reject every legal reference; and B1's imprecision is now removed **for protocols** and remains for globals, so the two halves disagree until B2b folds `prefix-out-of-scope` into `resolve-spelling`. **B2b landed 2026-08-08 — globals, `unsafe`, and the deletion of alias injection** (§9.3): `scope-lookup` splits into a *reference* resolver (local frames match the raw name; the global frame delegates to `globals-lookup-ref`, which resolves a qualified spelling through `resolve-spelling` and an unqualified one through the current namespace → the flattened set → `user`) and `scope-lookup-key`, the pre-B1 body, for the **8** of 59 call sites that hold an already-canonical key — every one of them paired with a `scope-define` (the `.nuch` replay, the C-header registrar, `emit-deferror`/`emit-extern` dedup, `cheader-yield-to-explicit-declare`, the REPL's three redefinition probes); 49 are references and 2 disappear. `inject-import-aliases`, `import-alias-one` and `alias-cinclude-collected` are **deleted** (64 lines, plus the C-header collect mode that fed them), so §1.1 defect #2 closes *by deletion* — there is no slice, hence no `is-local`/`ir-name` filter, and a prefixed `defvar`, `defconst` and enum member all resolve. The one guard that was **not** accidental, `sym-private`, had to be re-expressed as a resolution rule (`ImportBind.private` + `scope-frame-find-public`, plus a probe of the imported file's W5e `#pN/` key space for `unsafe/import-private`) — with the trap that an imported file with no `(ns …)` is in `user`, so for a `user` importer the two namespaces compare equal and a "sees its own private names" shortcut swallows the private probe. B1's `prefix-out-of-scope` is gone and `unbound-prefix-message` folded into `qualifier-scope-note` as its first tier, so there is **one gate and one note with two tiers** and the §9.2 disagreement is removed. `unsafe` becomes a genuine built-in namespace bound as an implicit prefix in every file; the seven `unsafe/*` strings leave `g-special-form-set` — measured correction to §8.3: that set is a **reservation**, not the dispatcher (`emit-list`/`node-type-call` compare interned head pointers and never consult it; its sole consumer is `name-existing-kind`), so removing them required `special-form-named` to resolve-then-roster or `(defn unsafe/cast …)` would have become legal and permanently shadowed. Six of UN-5's seven bare refusals were deleted from `emit-list` **and** `node-type-call` together (the lockstep) and reproduce their exact messages from a did-you-mean tier; the seventh, `unsafe-import-private`, is a different string from `import-private` and keeps its own arm. W1d loses a tier — a `prefix/name` over a cycle member now resolves, since a prefix names the *file* whose signatures and namespace the whole-graph prescan already recorded, so `cycle-prefix-message`/`g-cycle-prefixes` are deleted and the probe becomes an acceptance test — and `lib/nsdescribe.nuc`'s recorded gap ("a namespaced file cannot construct a box at all today") closes via the bare path's `user` fallback. Exactly the **seven** predicted matrix cells moved (`plain-fn`/`defvar`/`defconst`/`defenum-member` `zn/` ok→err, `defvar`/`defconst`/`defenum-member` `zx/` err→ok); eleven more changed message only, all the folded qualifier tier. `make bootstrap` at its fixed point, **441 tests pass, 0 fail**, and the chunk is **byte-for-byte inert on emitted IR** — 181/182 against a clean `HEAD` worktree (the 182nd being `w9-dyn-ns.nuc`, which `HEAD` rejects) and **182/182** against B2a's own post-fix IR, which isolates B2b. **New input to the A-vs-B decision (§12.7):** B2a's finding survives the larger sample — the per-registry probe cascade is only ~2 lines of registry-specific code (the rest is *policy* A would still have to write once), and the call-site audit is invariant under the choice — but two things now argue **for** A that §12 had not recorded: `unsafe` is a binding with **no registry at all**, which is the one shape B has no home for (three hand-written pieces under B, seven table rows under A); and privacy is a uniform naming concern enforced on `Sym` and nowhere else, so `defstruct-`/`defunion-`/`defmacro-`/`defprotocol-` are accepted spellings whose privacy **nothing enforces** — the same failure mode as the unreachable `NK-PROTOCOL`, found independently in a second uniform concern. **B5 landed 2026-08-09, out of order and upgraded (§13.4): the shared binding interface.** The A-vs-B decision was taken at the B3′ boundary and is **B's path with B5 as one interface (B+)** — because two of §12's claims did not survive contact (cutting a kind over is dominated by a *provenance* audit A does not remove, and A does nothing for the *wrong-phase* bugs a prescan-minted key creates) while the two findings that argue for A are both about uniform concerns being under-delivered per kind, which is an argument for one **interface**, not one storage table. Built: one table, thirteen rows (§1's eleven registries plus the two correctly-global name sets), each with a `noun`, the `NK-*` it reports, and three columns — `collides` / `name-keyed` / `reregisterable` — that make a kind's *non*-participation in a shared concern a stated fact instead of a silent omission. **The row order is the resolution order**, walked by `name-existing-kind`, `emit-dispatch` and `node-type-call` (the last through a *window* that stops at the rows which yield a type — the lockstep expressed as a different bound on the same call, not a second copy of the order). **Encoding is a kind tag + `case`, deliberately not fn-pointer struct fields**: three open W9 defects sit in that path (`(= h null)` does not compile for a `TY-FN` value, `type-size` has no `TY-FN` case so a fn-pointer slot emits `align 1`, `coerce-int-val` has no `raw`→`TY-FN` case), so a vtable-shaped table would have had to fix all three and move the IR of every fn-pointer program to buy nothing a closed thirteen-row dispatch needs. **Unifying the two orders required changing the question, not picking a winner:** taking `emit-dispatch`'s order directly *weakened* the guard, because every definer registers its own name in a prescan before its own guard runs, so `(defn Shape …)` over an existing `defprotocol Shape` found the `Generic` it had just registered, matched `NK-FUNCTION`, and compiled clean (measured). `guard-name-kind` now asks for the first binding whose kind is **not** the one being defined — order-independent, strictly stronger, and the reason one shared order is possible at all; the visible cost is that a cross-kind clash is reported at whichever definer is *emitted* first, so two existing pins were re-pointed (still rejections, different blame line and noun). `NK-PROTOCOL` is returned at last, which needed a **prescan reorder** as well as a row (`prescan-struct-names` registers a name-only StructDef without guarding, so it won every race — exactly why §2.5's clash was reported at the protocol's line as "a type"). **The privacy hypothesis held**: the four `Sym`-less private definers had no carrier at all and the dispatch loop's own comment claiming `prescan-protocols` honored the flag was false; the rule is now one function (`binding-visible` over `priv` + `src-ns`) called from the six *reference* lookups — not from `binding-probe` alone, which would have left `parse-type-name`, i.e. every `:T` annotation, unguarded — and provenance must be captured at **emission**, never in the prescan, because `prescan-imported-types` does not apply an imported file's leading `(ns …)` and the resulting wrongly-namespaced private entry hid itself from `emit-defstruct`'s own `lookup-struct`, which then registered a second StructDef under the same name. The did-you-mean renders a candidate through `src-ns` into a spelling the file can actually write, closing #9. `make bootstrap` at its fixed point, **453 tests pass, 0 fail**, **no matrix cell changed status** (two changed message, both #9's fix), and **181/182** `examples/`+`lib/` files emit identical IR against a clean `HEAD` worktree. §14.7 tells B3′ and B4 exactly what to fill in; §14.8 records, honestly, the four places the interface is a *worse* fit than eleven bespoke registries — the conformance relation has no single-name probe at all, a payload-free row still needs a sentinel, `binding-find` now takes six parameters to serve three callers wanting three slices of one order, and a table walk costs a hot path what a hand-written ladder short-circuited. **B3′ landed 2026-08-09 — type identity is namespaced (R1, defects #4 and #7), and only B4 remains.** All six type rows re-keyed on the canonicaliser in **one** step, because the planned struct/union-first split did not survive the audit: `parse-type-name` is the single entry for every `:T` spelling in the language and its cascade consults all six registries in one function, so cutting one over alone would have left `(ref zzz/Fox)` refused while `(ref zzz/Shape)` still resolved — defect #4 half-closed with no way to say which half. That also corrects §14.7's plan, which said each row's `binding-probe` arm is "the *only* place its key policy is expressed": `parse-type-name` calls each registry's lookup **directly**, the same fact §14.4 recorded for privacy, reappearing for keying. Five *reference* resolvers share one candidate-key walk, the old bodies become the *key* lookups, `parse-type-name`'s `strip-ns-qualifier` is deleted (the function stays with 6 live call sites, none of them a resolution — §8.1's "10 call sites go away" was right about the policy and wrong about the count), and `register-struct` derives `ir-name`/`ir-prefix` from **the key's own namespace** rather than `g-current-ns`, since a stamp or an import runs under someone else's — so `(ns dp) (defstruct Fox …)` emits `%dp__Fox` and `--emit-cheader` composes the same prefix (two namespaces' `Pt` would otherwise emit one C typedef). `export` is generalised to the type and protocol rows but **not** by re-registering — a type's payload *is* its identity, so a second `StructDef` would make the facade's name a second type; it is a re-export **alias** table, which corrects §11.6 in place. The new mechanism is **`g-type-key-ok`**, a scoped *synthesis-region* permission on `g-defvar-soft`'s shape: `type-spelling` renders a `Type` back to its canonical name and the compiler re-parses that string in regions (template stamp, monomorphized body, `Self` substitution, stored conformance arg, `.nuch` replay) whose file is routinely not the file that produced it, so the resolvers take an exact-key fallback only while armed and only after the reference walk misses. **Its honest weakness is that a dynamic-extent permission has no compile-time evidence its extent is complete, and a missing arm is a false rejection of a legal program** — three were missing, each adjacent to one that was present, and all three were found by *running* a namespaced type through ordinary idioms rather than by reading: `tmpl-conformance-check-one` (the stamp-time check of a template-level `(extend (Vector T) (Seq T))` — the widest, since every collection in `lib/` carries one, so **no namespaced type could be a collection element**), `generic-instantiate` (the stamped *signature* parse; the *body* was already armed), and `resolve-param-type-bound` (the shared substitute-and-reparse helper behind want-driven `vector-new`). A fourth fix was not an arm but §9.2's rule reasserting itself — `emit-extend` canonicalized its subject and then re-parsed the *canonical* name "for a clean diagnostic", asking the scope question twice, so `(extend gx/Pt P)` was refused as an unknown type; it validates the authored spelling now. `make bootstrap` at its fixed point with **no reconverge**, **463 tests pass, 0 fail** (the new `b3a-ns-type-in-collection` links and runs the composite idiom and is verified to *fail* on a `HEAD`-built compiler), **182/182** `examples/`+`lib/` files identical against a clean `HEAD` worktree, all **180** fixtures byte-identical on stderr as well as IR, and `type-annot nope/` — the last wrongly-`ok` matrix cell — is closed. **Two defects are left measured and open (§9.4):** a `(dyn P)` box's identity is keyed on a spelling-derived name, so a *prefix* spelling splits it — `examples/w9-dyn-ns.nuc` emits **two** `{data,vtable}` `StructDef`s for one protocol today, and a library taking `(dyn Describe)` cannot be called from a consumer holding `(dyn dpx/Describe)` (it fails as a raw LLVM parse error with no source location); the obvious fix, canonicalizing through the reference resolver, is *wrong* because `dyn-require-protocol` asks the scope question against the stored name, so identity and admission need different data and admission has to move to the annotation site — which is also §9.2's fix for the tenth defect. And a `(dyn P)` value is silently accepted where `(dyn Q)` is required for genuinely different protocols, caught only by LLVM's parser — pre-existing Stage 13 TE-3 behaviour, namespace-independent, and the reason the identity split is not louder. **B6 landed 2026-08-09 — both closed, and only B4 remains.** Identity and admission were separated: `dyn-proto-key` (`src/nucleusc.nuc`, replacing `protocol-canon-name-ns`) keys a box on the protocol's canonical name derived from `resolve-spelling` and **no registry** — consulting no registry is the load-bearing property, not an economy, because the root file's `prescan-defn-signatures` runs *before* `prescan-imported-signatures` (and pass 2's traversal is pre-order), so a registry probe answers "not found" at prescan and "found" at emission and the two keys then disagree **inside one program**, which is worse than the bug; the one probe kept is `-exact` on the current namespace's key, phase-stable because a file's own `prescan-protocols` precedes its own signature prescan, and it reproduces the old answer for every bare spelling, which is why 181 of 182 `examples/`+`lib/` files emit byte-identical IR. Admission moved to the **annotation site** as a deferred worklist (`DynAnnot` carrying `{spelling, path, line, ns, imports}`; `dyn-annot-record` → `drain-dyn-annots`, which restores the file's environment and asks `dyn-require-protocol` on the *authored spelling*), because once a box stores a canonical name, asking the scope question against that stored name refuses **every legal box** — the failure mode §9.4 predicted for fix (a), and measured at `HEAD`, where passing a consumer's value into a namespaced library's `(dyn P)` parameter already died `'b6dp/Describe' is not a declared protocol`. Box construction is therefore downgraded to a key lookup (`dyn-resolve-protocol`). **The drain runs after *emission*, not after the prescans as §9.4 proposed**, because a `.nuc` imported by **string path** is walked by no prescan at all — which also records §9.2's alternative fix (b), "move the imported signature prescan ahead of the root's own", as the wrong recommendation: no reordering of prescans reaches a file no prescan visits. Completeness is **asserted** (`main` checks cursor == count) rather than argued, which is the one form of evidence a dynamic-extent worklist can offer. The adjacent defect closed too: `box-require-same-kind` is the `type-eq` the erased-slot coercion never had, and there turned out to be **two** coercion sites — `maybe-box-into-slot` (binding, `return`) and a separate pair of blocks in `emit-call-with-args` for arguments — with the argument one the dangerous half, since the SysV ABI splits the fat pointer into two i64s at the call so nothing caught it and the program ran against the wrong vtable. **Three measurements corrected the design in place:** `examples/w9-dyn-ns.nuc` was never a witness for the split (it imports two *different* libraries in namespaces `dp`/`dp2`, so two box types are correct there — only their *names* were wrong, keyed on the consumer's prefixes; a cross-boundary defect cannot be witnessed inside one file, and `run_b6_dyn_cross_ns` is the real witness, which **links and runs**); the documented failure direction was the opposite of the one that actually fires first; and "all four `protocol-dyn-annot` cells must move" is **three** — `zx/` is the one spelling the consumer may legally write and moving it would be precisely the false rejection the deferral prevents. `make bootstrap` at its fixed point with **no reconverge**, **467 tests pass, 0 fail** (four new, all verified to fail on a `HEAD`-built compiler; two of them compiled, linked *and ran* there), all **180** existing fixtures byte-identical on stderr, and the only IR that moved in the tree is `examples/w9-dyn-ns.nuc`'s box/vtable rename `dpx`/`dpx2` → `dp`/`dp2`. **Open after B6:** the `import-use`-flattened *bare* spelling of a namespaced protocol still keys bare (closing it needs a phase-complete protocol registry — registering every reachable file's protocols in pass 1 is the candidate, with `guard-name-kind` ordering as the risk), a `.nuc` imported by string path is walked by no prescan (wider than `(dyn P)`), and `set!`/`.set!` into a box slot reaches neither coercion site.
- [stage15-stress-test/implicit-conversions.md](stage15-stress-test/implicit-conversions.md) — **ruling + fix (2026-08-15), Stage 15 W9 items 42 and 47 — DONE.** The ruling: implicit conversions **do not compose** — one conversion, built-in or user, never both — so an `i64 → ptr` `defcast` is not reached by the `i32` literal in `(take 0)`. C++'s answer, and it cost zero lines, because measurement showed the compiler already refused composition in all five positions probed; the defensible reasons are that chaining turns a pair lookup into a **path search** over a registration-ordered `Vector` that `lookup-cast-rule` cannot rank, and that a rule from the narrow type is spellable so nothing becomes unreachable. **The item's substance was the question the measurement forced** — *where* the registry is consulted — whose answer was two of nine typed slots: `safe-coerce-val` is called only by the call-argument loop and `as`, while `let`/`with` init, both returns, `.set!`, `aset!`, struct-literal fields and union payloads call `coerce-int-val`, which knows every built-in conversion and nothing about the user's, and so refused a rule **on its own exact registered pair** (filed and fixed as item 47). Fixed at the chokepoint — `coerce-int-val`'s single *final* fallthrough, as distinct from the earlier `(return null)`s that refuse a specific pair — so all nine slots gain it at once and a tenth inherits it; additive by construction (the rule runs only where the caller was about to `die-at`) and by measurement (365 corpus programs, 0 IR diffs, 0 new errors, 0 changed error texts). The refactor deleted `safe-coerce-val`'s inline copy rather than duplicating it, and deliberately did **not** route the other slots through `safe-coerce-val`, whose equal-type-*kind* short-circuit would let a `(ptr Foo)` initialize a `(ptr Bar)`. A **near-miss note** (staged in `g-diag-note`, rendered by `die-at`/`report-at` like `g-mono-context`) names the rule that almost applied, which is what makes the ruling distinguishable from "your rule was never registered" — the failure mode that let `examples/implicit-cast.nuc` document a cast that never fired for its whole life, and that made `as`'s "use unsafe/cast" advice actively wrong. Incidental correction now in `conventions.md`: the `abi.nuc` → `nucleusc.nuc` up-call this needed is called an unresolvable cross-import forward reference by two entries there and one comment in `coerce-int-val`; it compiled on the committed boot compiler first try, the signature prescan having made those notes stale.


## Agent feedback

Some design documents are structured as a conversation between a designer and a robot. Agents tasked with responding to a design document should add their feedback to the robot sections.

Agents encountering new design documents should add them to the above list.

## Pre-release

Nucleus is currently in pre-release development. Breaking changes are expected without warning: at this point Nucleus is only used for its own compiler (and the examples), so it is acceptable to make breaking changes without regard to other projects, because there are none.
