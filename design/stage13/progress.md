# Stage 13 Progress: Lambdas and closures

Detailed task table for the L0–L9 lambda/closure implementation. Design:
[lambda.md](lambda.md); build order / acceptance gates: [lambda-prompt.md](lambda-prompt.md).
**129 tests pass; `make bootstrap` is a byte-identical fixed point.**
(99 auto-discovered `examples/*.nuc` tests + 30 named fixture checks.)

**Follow-ons:**
- [closure-enhancements.md](closure-enhancements.md) — feature enhancements:
  **CE-1** named closures, **CE-2** mutable capture, **CE-3** owning-closure
  export — all **Done** (see tables below). **CE-4** storable closures is now
  **implemented** via [type-erasure.md](type-erasure.md) (`BoxedFn` /
  `(dyn P)`) — see the Type erasure table below; the CE-4 Option 3 "design
  exploration only" designation is **superseded**. Also recorded: the
  `cfn`-of-pointer escape imprecision (deferred nested-region precision, unchanged).
- [functional-refactor.md](functional-refactor.md) — the dogfooding refactor of
  the compiler and libraries around named closures, eager sequence combinators,
  `Vector`, and iterators. Build order: phases **R1 … R4** (depends on CE-1).
  Not yet started.
- [type-erasure.md](type-erasure.md) — the **shared type-erasure machine**: one
  two-word fat pointer + static per-type vtable that realizes `BoxedFn` *and*
  `(dyn Protocol)` as two instantiations of the same mechanism. **Done (TE-0 …
  TE-7)** — see the Type erasure table below.

| Phase | Status | What landed | Key functions / files | Notes / surprises |
|---|---|---|---|---|
| L0 — Ground-verify | Done | Read-only map of the escape/`invoke`/conformance/struct-template/worklist/`Drop` machinery; build assumptions confirmed | (read-only survey) | Taint is a `Scope*` (not boolean) → region tagging a natural extension; **no synthesized `drop` exists** (closures generate the first); a lifted top-level `defn` can be queued from inside expression emission and drained like a generic stamp |
| L1 — Escape generalization | Done | `addr-of`/`.&` on frame-local storage taints a region-tagged pointer; upward escape (return / store-out) rejected; reference/pointer params excluded; `move`/copy/`deref`-out clear taint | `emit-addr-of`, `node-type-addr-of`, `emit-return`, `pkind-flow-check`, `emit-move`, `emit-field-addr`, `emit-ptr-add`, `emit-cast` | **Only non-additive phase.** Measure-then-flip found no genuine `return &local` site in the compiler; re-converged byte-identical. `let` gains no drop/lifetime semantics — pointer-provenance only |
| L2 — `Clone` | Done | `Clone` protocol + automatic structural conformance (bitwise) for trivially-copyable types; hand-written `clone` for owning types; `Drop`-without-`Clone` is not `Clone` | `blanket-conforms`/`blanket-lookup` (alongside `Any`/`Struct`), `type-conforms-drop` | Modeled on built-in blanket rules, **not** user `extend` |
| L3 — `fn` | Done | Anonymous function → bare function pointer; free-variable/capture analysis; rejects enclosing-local capture with a directed error; lambda-lifts to a gensym top-level `defn` | `fn-capture-walk` / `fn-capture-check`, `emit-fn`, top-level worklist | First cut forbids **all** enclosing-local capture (the inlinable-const-local relaxation deferred). `fn` is the only form emitted to C headers |
| L4 — `vfn` + env + `invoke` | Done | Anonymous env struct `__vfn_env_N` (one field per capture) + `invoke` method of the closure's arity; per-field `clone`; synthesized `drop` iff any capture is `Drop`; POD-only = non-owning by-value | `fn-make-env-struct`, `fn-make-invoke-method`, `fn-rewrite-captures` (+`-list`/`-rest`/`-from`/`-let-binds`), `fn-emit-env-value`, `fn-make-drop-method`, `fn-force-generic-mangled`, `fn-caps-count`, `fn-env-ref-type`/`-ptr-type`, `emit-vfn` | **Direct env emission, not a struct literal** — `emit-struct-lit` misreads `(clone (addr-of x))` as `clone:=`. **`fn-force-generic-mangled` is load-bearing** (lone-`Drop` `@drop` hijack). `self` prepended onto the whole params spine. 112 tests |
| L5 — `mfn` | Done | Move-capture via a single mode flag on `fn-emit-env-value`; reuses all L4 machinery verbatim; `move` sink disarms + marks consumed | `emit-mfn`, `fn-emit-env-value` (mode 1) | Export-from-`with` confirmed at IR level: move disarms source cleanup → return sound, no escape error. Minimal factoring — one mode flag, no forked emitter |
| L6 — `cfn` | Done | Reference-capture; env is a struct of pointers + stored `AllocHandle`; takes a bare allocator operand; conforms to `Drop` to free the env; inherits each captured reference's region | `emit-cfn` | Region inheritance makes escape-checking fall out of L1's existing sinks. A `cfn` capturing only `(ref …)` params/globals may be returned freely |
| L7 — Structural conformance derivation | Done | A closure/`fn` flowing into `&where ((P …) V)` gets a synthesized forwarding conformance by matching `invoke` against `P`'s single required method; bound args read off `invoke`'s signature | conformance registry; bare-`fn` types interned as `__fnty_N` | **Recognized set = {`UnaryFn`, `FoldFn`}** — resolved toward the narrow side of lambda.md's open question. Idempotent; obeys one-conformance-per-`(type,protocol)` coherence |
| L8 — C interop | Done | Public `defn` mentioning a closure env type (return or param) excluded from `--emit-cheader` (an `/* ... exposes a closure type ... */` comment is left) + warns at its definition; `fn`-pointer signatures emit normally | `cheader-mentions-closure` (`__vfn_env_` prefix match); the defn-definition warning | Detection by **name prefix** — the cheader path is pre-codegen (AST-only), so a `StructDef` flag would be invisible. Warning gated to public, non-synthesized defns |
| L9a — Examples / tests | Done | `examples/closures.nuc` (all four forms, each passed inline to `reduce`); `tests/fixtures/closure-escape.nuc` (L1 negative); `tests/fixtures/closure-cheader.nuc` (L8 omission + warning + fn-pointer emission) | `examples/closures.nuc`, `tests/expected/closures.out`, the two fixtures | 117 tests pass. Closures are passed inline because anonymous env types can't be named in a binding (see limitations) |
| L9b — Docs / progress | Done | `docs/special-forms.md` (closure rows + `invoke`-lowering note + sharpened escape two-concerns split), `docs/generics.md` (`Clone` + structural-conformance section), this file, `design/progress.md` Stage 13 row + narrative, lambda.md "Robot — implementation status" | (docs/progress only) | No code or test changes |

## Pre-existing limitations (not closure bugs)

These reproduce on the boot `bin/nucleusc` independent of Stage 13, and cap what
is runnable end-to-end (IR-level-verified-only cases noted). They are flagged for
future compiler work, **not** as Stage 13 defects:

1. ~~**By-value struct return/passing corrupts** via an `emit-struct-ret` ABI bug
   (store ptr / `sret` instead of memcpy) → an owning/`Drop` env struct can't
   round-trip by value through a binding or a return.~~ — **lifted by CE-3**
   (`emit-struct-ret` now branches on `TY-PTR` to copy bytes via typed load/store;
   the symmetric arg path in `emit-call-with-args` emits a first-class load when
   `ptype` is `TY-STRUCT` and the arg is `(ref same-struct)`).
2. ~~**`with-drop-method` only arms `Drop` for `TY-PTR` bindings** (the `ptr:Res`
   idiom), not struct-value bindings → a `with`-bound owning closure's synthesized
   `drop` does not fire at scope exit. The "mfn exported from `with`, drops at its
   eventual scope" case is **IR-level-verified only**.~~ — **lifted by CE-3**
   (`with-drop-method` generalized to `TY-STRUCT`: struct-value bindings drop at
   scope exit and struct-value `Drop` captures can be moved into an `mfn`; the
   `mfn`-export case is now runnable end-to-end).
3. ~~**Anonymous env types can't be named in `let`/`with`**~~ — **lifted by CE-1**
   (below). A bare-symbol `let`/`with` binding with no `:type` now infers its type
   from the init value, so a closure can be bound to a name and called,
   `with`-dropped, or passed by name to a combinator.

A POD `vfn`/`mfn` over scalars is **fully runnable** (non-owning by-value env, no
`Drop`, passed inline) — `examples/closures.nuc` exercises this. The
owning-closure mechanism (synthesized `drop`, env conformance, move disarm) is now
**fully runnable end-to-end** — `examples/ce3-owning-closure.nuc` exercises
by-value owning-struct return, struct-value `with`-drop, and `mfn`
move-of-struct-value-`Drop`-capture together. **Remaining gap:** a `defn`
returning a closure *object* errors "unknown type" (the anonymous `__vfn_env_N`
can't be spelled as a return type) — the CE-4 env-naming gap, not a CE-3 defect.

## Feature enhancements (closure-enhancements.md)

| # | Status | What landed | Key functions / files | Notes / surprises |
|---|---|---|---|---|
| CE-1 — Named closures (`let`/`with` env-type inference) | Done | A bare-symbol `let`/`with` binding with no spelled `:type` adopts the type of its emitted init value, so a capturing closure (whose anonymous env type cannot be written) can be **named** — then called by name, dropped at a `with`-scope via the existing `TY-PTR` `with-drop-method` path, or passed as a **named** operand to a generic combinator. | `emit-let`, `emit-with` (`nucleusc.nuc`); inert reuse of `extract-name-and-type`, `coerce-int-val` (self-coerce no-op), `derive-closure-conformance` (`generics.nuc`) | **Additive / byte-identical, no refresh.** Gated to bare symbols with `ty == null` — the exact case that previously errored "missing `:type`" — so typed/destructuring bindings are untouched and the path is **inert under self-compilation** (the compiler has no untyped bindings). To infer, the init is emitted **before** the slot alloca only in the inference case (`pre-v` holder), preserving `g-tmp` ordering for the typed path. Conformance is **type-keyed** (`derive-closure-conformance` keys off the env spelling + recorded `invoke`), so a named operand resolves identically to an inline one. `examples/named-closures.nuc` (+ `.out`), 120 tests |
| CE-2 — Mutable capture | Done | Special-cased `set!`/`inc!`/`dec!` on a captured name in `fn-rewrite-captures`: mode 0/1 rewrites to `(.set! self c ...)` (field store); mode 2 (cfn) rewrites to `(ptr-set! (. self c) ...)` (store through captured pointer). `inc!`/`dec!` expand to read-modify-write: `(.set! self c (op (. self c) 1))` / `(ptr-set! (. self c) (op (deref (. self c)) 1))`. Non-captured targets fall through to the unchanged generic rewrite. | `fn-rewrite-captures` (`nucleusc.nuc`) — new `set!`/`inc!+dec!` special cases; `examples/ce2-mutable-capture.nuc`; `tests/expected/ce2-mutable-capture.out` | **Additive / byte-identical, no refresh.** No existing closure in the compiler mutates a capture, so the new branches are inert under self-compilation. `inc!`/`dec!` inside `if` branches each have a nested `let`, so the closing paren count differs by 1 from the `set!` case — a subtle balance point to note. 121 tests |
| CE-3 — Owning-closure export (by-value return) | Done | Fix (b): `emit-struct-ret` (`abi.nuc`) branches on `TY-PTR` to copy bytes via typed `load`/`store` (MEMORY) or `load reg0[/+8 reg1]` (COERCE1/COERCE2) rather than storing the pointer; `emit-call-with-args` coercion loop emits a `load` to first-class when `ptype` is `TY-STRUCT` and the arg is `(ref same-struct)` so `abi-arg-frag` (left unchanged) classifies correctly. Fix (c): `with-drop-method` + `emit-with` replace the `TY-PTR`-only gate with a `cond` over `TY-PTR`/`TY-STRUCT`; the `TY-STRUCT` branch synthesizes a `(ptr S)` query type, allocates `%name.drophandle.N = alloca ptr` pointing at the value slot (handle is `Cleanup.slot`/`sym cslot`; value slot stays `sym ir-name` for reads), and reuses `emit-drop-cleanup`/`emit-move` verbatim — `owns=1` on the struct binding is also what lets `emit-move` consume a struct-value `Drop` capture into an `mfn`. `emit-addr-of` gains `(sym moved)` guard (mirrors `emit-symbol-ref`). | `emit-struct-ret` (`abi.nuc`); `emit-call-with-args`, `with-drop-method`, `emit-with`, `emit-addr-of` (`nucleusc.nuc`) | **Bootstrap byte-identical, no refresh** — all CE-3 changes are inert under self-compilation. **+2 tests** (123 total): `examples/ce3-owning-closure.nuc` (by-value owning-struct return from `make-res` + `mfn` moving a struct-value `Drop` binding), `tests/fixtures/ce3-use-after-move.nuc` (negative: `addr-of` of a moved binding rejected). Note: `type-size` returns pointer size for structs so typed load/store is used — LLVM computes the real byte count; no `llvm.memcpy` decl needed. **Known boundary (CE-4 gap):** a `defn` returning a closure *object* still errors "unknown type" (anonymous `__vfn_env_N` can't be spelled as a return type); the by-value ABI and move/drop machinery are complete. |

## Type erasure — shared fat-pointer machine (type-erasure.md)

| # | Status | What landed | Key functions / files | Notes / surprises |
|---|---|---|---|---|
| TE-0 — Ground-verify (read-only map) | Done | Map of the fat-pointer/vtable insertion points: confirmed the fat pointer rides the CE-3 by-value-struct ABI (`{ptr,ptr}` = 16 B → `abi-classify` ABI-COERCE2), the per-env `invoke`/`drop` symbols are reproducible mangled names addressable as vtable slots, and no unified conformance-admission entry point exists (TE-3 must create one). | (read-only survey) | Confirmed inertness: the compiler boxes nothing (`grep BoxedFn\|(dyn` over `src/`/`lib/` = 0), so TE-1…TE-6 target byte-identical. Caveat recorded: register `__fatptr` **lazily** or every module's IR drifts. |
| TE-1 — Fat-pointer value kind + ABI | Done | Lazy, memoized `ensure-fatptr-sdef` registers one `{data:ptr, vtable:ptr}` `__fatptr` StructDef (16-byte `TY-STRUCT`) via `register-struct`, queues its `%__fatptr = type {ptr,ptr}` IR on `g-pending-unions` (no named deps → emits at next drain); `fatptr-type` memoizes the `TY-STRUCT` `Type*` over it. No new value-vs-pointer logic — the fat pointer rides the CE-3 ABI-COERCE2 path (`emit-struct-ret` `TY-PTR` typed copy / `emit-call-with-args` first-class load / `abi-arg-frag`) verbatim. | `ensure-fatptr-sdef`, `fatptr-type`, `g-fatptr-sdef`/`g-fatptr-type` (`union-registry.nuc`) | **Byte-identical, no refresh.** Lazy registration is load-bearing: it fires in **zero** modules during self-compilation (nothing boxes), so `%__fatptr` is emitted nowhere and the boot is byte-identical (verified: `grep '%__fatptr = type' build/nucleusc.ll` = 0). Eager init at startup would drift every module. |
| TE-2 — Static per-type vtable synthesis / dedup / addressing | Done | `ensure-vtable-for(concrete-ty, proto, line)` emits one `@__vt.<type>.<proto> = internal constant { ptr, ptr } { ptr <invoke>, ptr <drop-or-null> }` to `g-out` (current module, alongside the thunks it references — forward global refs are legal), memoized per `(type-spelling, proto)` via parallel arrays (mirrors `g-fnty-*`). `vtable-invoke-irname` reuses `derive-closure-conformance`'s env-receiver scan over the `invoke` generic to read the env's pre-mangled `@invoke.p<Env>.<toks>` `ir-name`; `vtable-drop-irname` delegates to `with-drop-method` (or null for a POD/non-owning type). Symbol routed through `sanitize-for-ir`. | `ensure-vtable-for`, `vtable-invoke-irname`, `vtable-drop-irname`, `vtable-memo-lookup`/`-put`, `g-vtable-*` (`nucleusc.nuc`) | **Byte-identical, no refresh.** No caller yet (TE-3 supplies one), so zero `@__vt.*` constants emitted during self-compilation (verified). The thunk addresses are the *already-synthesized* per-env `invoke`/`drop` methods — the vtable just takes their addresses; no new thunks written. |
| TE-4 — Box `Drop` + forwarding-through-vtable conformance | Done | **Box Drop:** the box is registered Drop-conforming via one SHARED hand-emitted thunk `@__boxedfn_drop(ptr self)` (lazily emitted once per module to `g-out`): null-guard `data`, indirect-call the vtable drop slot (the env's own field-drops; null for a POD env), then `free(data)` — symmetric recovery of the TE-3 default-libc allocator (no per-box handle). `ensure-box-drop-fn` (called from `emit-box-closure`) records the `(box-sdef, Drop)` conformance (so `type-conforms-drop` is true) and emits the thunk; `with-drop-method` short-circuits a box `ty` (via `boxedfn-drop-target`) to a synthetic Method carrying `@__boxedfn_drop`, so `emit-with`'s existing CE-3 struct-value cleanup path arms the scope-exit drop unchanged. **A cfn env's vtable drop slot is forced null** (`vtable-drop-irname` detects the leading `__alloc` field) — a cfn drop FREES its block, which the box also frees, so leaving it would double-free; the original cfn env then leaks (documented cfn-box ownership gap). **Forwarding:** `emit-box-invoke` lowers `(invoke box args…)` / `(box args…)` to an indirect call — `extractvalue` data+vtable from the first-class box, load invoke slot (vtable field 0), `call ret invfn(ptr data, args…)`, args coerced to the box signature carried on the box `Type`. Intercepted at the top of `emit-invoke-with-callee` and in `emit-callable-value` (before the get/`_get` split). `(dyn P)` multi-slot forwarding is the same indirect-call shape (TE-6). | `ensure-box-drop-fn`, `boxedfn-drop-target`, `emit-box-invoke`, `g-boxdrop-emitted` (new); `with-drop-method`/`vtable-drop-irname`/`emit-box-closure`/`emit-callable-value`/`emit-invoke-with-callee` (changed) — all `nucleusc.nuc` | **Byte-identical, no refresh** (`stage1.ll == stage2.ll`, 123 tests green): every hook gates on `boxedfn-canonical`/`boxedfn-drop-target` returning null in a box-free unit. Scratch-verified IR+runtime: `(b 5)`→15, `(invoke b 10)`→30 via vtable; POD box frees its env block once (`MALLOC_CHECK_=3` clean); a vfn box owning a `Drop` capture runs the cloned field's `drop` through the vtable slot then frees — 3 user mallocs / 3 user frees, each once, no double-free/leak. **Per-module caveat** (shared with the TE-2 vtable): `g-boxdrop-emitted` is global, so a box dropped in a second CT-JIT module would miss the `internal` define — correct for the single-module batch case. |
| TE-3 — Boxing coercion at assignment into an erased slot | Done | At a `(BoxedFn …)` `let`/`with` binding, `return` slot, or call argument, a concrete capturing closure (a `(ref Env)` value from vfn/mfn/cfn) is heap-boxed into the shared fat pointer: (1) `admit-erased-conformance` — the **single unified admission point** — admits structurally (the env's `invoke` non-self signature must match the box's `(params…) ret`, carried on the box `Type`) then falls back to nominal `conformance-lookup` (dormant: only fires when a proto-name is passed, i.e. TE-6); (2) the env is moved **by value** into a fresh heap block (typed `load`+`store` of the whole aggregate = the CE-3 by-value move) sized `sizeof(Env)`, allocated through `alloc-handle-alloc(default-allocator, …)`; (3) `ensure-vtable-for(env, "Fn")` resolves the per-env vtable; (4) the fat pointer `{data=heap, vtable=&__vt…}` is alloca/store/load-built and yielded. Chokepoint `maybe-box-into-slot` wraps the four coercion sites and is a no-op for any non-box target. **Closes the CE-4 env-naming return gap** — `make-adder:(BoxedFn (i32) i32)` returns a boxed closure by value (fat ptr lowered to `{ i64, i64 }` via the CE-3 ABI). | `admit-erased-conformance`, `emit-box-closure`, `maybe-box-into-slot`, `closure-invoke-method` (`nucleusc.nuc`); call sites in `emit-let`/`emit-with`/`emit-return`/`emit-call-with-args` | **Byte-identical, no refresh** (verified `stage1.ll == stage2.ll`, 123 tests green): the compiler spells no `(BoxedFn …)`, so `boxedfn-canonical` returns null at every site and zero box IR is emitted. **Allocator decision (v1 open question):** the two-word fat pointer has no room for an `AllocHandle`, so the box does **not** thread the closure's own allocator — it boxes through the process-default libc allocator (a process-lifetime global), which TE-4's box-`Drop` recovers identically / frees directly. A future per-allocator box would prepend a handle-header word. **Scope:** only capturing closures (env case); a non-capturing closure decays to a bare `fn` (TY-FN) and its box is deferred to TE-5 (needs a synthesized forwarder, not an env move). The `(BoxedFn (params…) ret)` **type-parser surface** (TE-0 had filed it under TE-5) landed alongside in `union-registry.nuc` (`boxedfn-type`/`boxedfn-canonical`/`boxedfn-memo-*`/`boxedfn-sig-token` + a `parse-type-from-node` `'BoxedFn` case) to make TE-3 testable — **TE-5 should not re-add it.** |
| TE-5 — Instantiation A: `BoxedFn` surface + live use cases | Done | **Type-parser surface** for `(BoxedFn (params…) ret)` already landed in TE-3's `union-registry.nuc` pass (`boxedfn-type`/`boxedfn-canonical`/`boxedfn-sig-token`/`boxedfn-memo-*` + `parse-type-from-node` `'BoxedFn` case); a 2-field `{data,vtable}` `TY-STRUCT` named `__boxedfn.<sig>` with the Fn signature stamped on the `Type`'s ret/params/num-params. **Bare-fn (non-capturing) box** (`emit-box-bare-fn` / `ensure-fnfwd-vtable`): synthesizes a per-signature forwarder thunk `@__fnfwd.<sig>` (heap cell holds the fn pointer; the thunk loads it and indirect-calls); vtable = `{forwarder, null}`. **Four live cases** in `examples/boxedfn.nuc`: (1) bare-fn box; (2) `(Vector (BoxedFn (i32) i32))` of heterogeneously-captured closures; (3) struct field of `BoxedFn` type; (4) `defn` returning `(BoxedFn …)` — closing the CE-4 env-naming gap. Allocator: boxes through the process-default libc allocator; `cfn` env's vtable drop slot is forced null (double-free avoidance; cfn-box ownership gap documented). | `emit-box-bare-fn`, `ensure-fnfwd-vtable` (`nucleusc.nuc`); `boxedfn-type`/`boxedfn-canonical`/`boxedfn-sig-token`/`boxedfn-memo-*`, `parse-type-from-node` `'BoxedFn` case (`union-registry.nuc`); `examples/boxedfn.nuc` | **Byte-identical, no refresh.** 126 → 127 tests (boxedfn example auto-discovered). C-ABI: `cheader-mentions-closure` (`src/cheader.nuc`) extended to detect `(BoxedFn …)` and `(dyn P)` type-AST heads — a public `defn` mentioning a box type is excluded from `--emit-cheader` with an explanatory comment and warned at definition site (L8 precedent generalized). Fixture `tests/fixtures/box-cheader.nuc` + `l13-cheader-*` checks cover it. |
| TE-6 — Instantiation B: general `(dyn Protocol)` | Done | **Type-parser surface** in `union-registry.nuc` (`dyn-type`/`dyn-canonical`/`dyn-proto-of`/`dyn-memo-*` + `parse-type-from-node` `'dyn` case); a distinct 2-field `{data,vtable}` `TY-STRUCT` named `__dyn.<P>` with the protocol name in a parallel registry array. `ensure-vtable-for` generalized: the `"Fn"` branch is unchanged; any other protocol builds the vtable slot from the concrete type's declared `extend P` method impl (`dyn-vtable-method-irname`, mirroring `proto-sigs-resolve`). v1 vtable layout is `{method0, drop}` — same shape as BoxedFn; `@__boxedfn_drop` is reused verbatim. Boxing: `emit-box-value` + `emit-box-struct-move`; nominal branch of `admit-erased-conformance` active (`conformance-lookup`, deriving if needed). A `(dyn P)` method call `(method-name box args…)` lowers to an indirect vtable call (`dyn-method-slot`/`emit-dyn-forward`, intercepted in `emit-generic-call`). **B2 realized:** a `(dyn P)`-typed return type is now a concrete fat-pointer type, so `defn` returning `(dyn P)` is legal (the "unknown type" rejection at an unbound protocol name is resolved). Heterogeneous `(Vector (dyn P))` works. Example `examples/dyn-protocol.nuc`: protocol `Describe`, two POD types `Cat`/`Dog`, a `(dyn Describe)`-returning `defn`, by-value `(dyn Describe)` param, heterogeneous Vector iterated/dispatched/dropped once each. | `dyn-type`/`dyn-canonical`/`dyn-proto-of`/`dyn-memo-*`, `parse-type-from-node` `'dyn` case (`union-registry.nuc`); `dyn-vtable-method-irname`, `dyn-method-slot`, `emit-dyn-forward`, `emit-box-value`, `emit-box-struct-move`, updated `ensure-vtable-for` and `admit-erased-conformance` (`nucleusc.nuc`); `examples/dyn-protocol.nuc` | **Byte-identical, no refresh.** 127 → 129 tests (dyn-protocol auto-discovered; boxedfn + dyn-protocol + box-cheader fixture = +6 tests over baseline). **v1 scope limits** (natural rejections with clear messages): multi-method protocols (drop-slot index conflict); parametric protocols `(dyn (Seq E))`; protocols whose non-receiver params/return mention `Self` (object-safety — fail to parse); multi-protocol boxes. `clone` vtable slot is reserved but omitted; boxes are move-only. |
